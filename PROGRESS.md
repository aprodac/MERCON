# MERCON — Project Progress (Living Status)

**This is the single source of truth for "where is the project."** Last updated: **2026-09-22** (**Error Console: auto-reopen on recurrence**: `services/errorCapture.ts`'s upsert previously never touched `status` on a repeat occurrence, so an `ErrorEvent` manually marked `Resolved` that fired again stayed shown as `Resolved` in `/settings/error-console` — silently recurring, invisible without someone manually reopening it. Fixed: the upsert now reads the existing row's `status` first and, if it was `Resolved`, resets it to `New` on the new occurrence (`count` still increments as before). Full traffic-verified auto-resolve (confirming a fix via real successful requests on the same route, not just time-based silence) was scoped but deliberately not built — decided as a bad long-term tradeoff for this project's current error volume: it needs new per-request success-tracking middleware, has no reliable positive signal for client-side (`web`-sourced) errors without further client instrumentation, and risks eroding trust in the console via wrong auto-closes) · Previously (**Driver payout unification**: the mobile driver app's "Driver Charge" figure was wrong because it was computed by three disagreeing formulas — the mobile API (`mobileTripController.ts`'s `formatMobileTrip`) picked `driver_payout ?? driver_charge ?? trip_charges ?? quotation?.driver_payout` directly off the row, ignoring co-driver splits, 3PL subcontract cost, and extra driver payments, while the web dashboard already ran everything through `calculateBackendTripFinancials()` in `backend/api-server/src/utils/tripFinancials.ts`; and the mobile app itself had two more copies of the same ad-hoc picking logic (`getTripChargeValue` duplicated in `DriverChargesScreen.tsx` and `TripsScreen.tsx`). Fixed by making the mobile API call `calculateBackendTripFinancials()` too (added `subcontract` to its trip include so 3PL cost resolves) and collapsing both mobile-side duplicates into one `getTripChargeValue()` in `lib/trips.ts` that just reads the server-computed `driver_payout` field instead of re-deriving it) · Previously (**Round-trip stop data corruption fix + timeline consolidation**: found and fixed the real cause of a round trip showing a different route on web/mobile/wizard — a data-corruption bug in `bulkImportTrips` that duplicated the origin/destination stop on every trip created through the wizard, plus consolidated 3 disagreeing client-side route-timeline algorithms into one server-computed source — see §8) · Previously (**Error-handling audit Phase 2**: added error_events table + Admin Error Console (backend + web) — see §7) · Previously (**Backend error-handling audit Phase 1**: fixed the two client-facing stack-trace leaks and the hardcoded Gemini key fallback, added request-ID correlation + process crash handlers — see §7) · Previously (**Health-check/observability hardening**: a
`cpus` default from the Docker-hardening pass earlier the same day (api 1.5)
broke the next dev deploy — the VPS only has 1 CPU and Docker hard-rejects any
single service's `cpus` above the host's core count; lowered defaults
(postgres 0.5, api 0.75, frontend 0.25). That incident going unnoticed until
someone read raw logs motivated closing three gaps: (1) ported `ci-cd-dev.yml`'s
real post-deploy health gate (poll container health + curl `/health`, fail the
job on either) into `ci-cd.yml`, which previously just slept 8s and printed
logs unconditionally regardless of outcome; (2) added the missing
`mercon-frontend` Docker healthcheck (`/healthz` in `nginx.conf` + `wget`-based
`healthcheck:` in `docker-compose.yml`) — it was the only one of the three
services without one; (3) added `.github/workflows/backup-db.yml` — nightly
Postgres `pg_dump` (reusing the command already proven in
`sync-prod-from-dev.yml`) to `/var/backups/mercon` with 7-day local retention
plus a 14-day off-host copy via `actions/upload-artifact`, since no automated
backup existed anywhere before this. A 4th phase (opt-in failure-notification
webhook) is designed but not implemented, pending the owner naming a webhook
target. Full writeup: `docs/progress/2026-09-19.md`.) · Previously
(**Docker hardening**: audited the 3-container
deploy and confirmed the db/api/frontend split is already correct — no case for
more containers. Added `mem_limit`/`cpus` caps to all three services in
`docker-compose.yml` (env-overridable), switched `backend/api-server/Dockerfile`
to run as non-root `node` user, and removed a hardcoded Gemini API key fallback
from `ci-cd.yml` (key must be rotated — it's in git history). Flagged but did
NOT change: uploads still bind-mount under host `/tmp`, which needs a manual
VPS-side data migration before the default path can safely move.) · Previously (**SuperAdmin Role & Vendor Governance System**: Created a formal `SuperAdmin` role for the platform provider / vendor company. Updated Prisma schema `enum Role` and `@mercon/shared-types` `UserRole` to include `SuperAdmin`, with permission inheritance allowing SuperAdmin users to access all `Admin` features automatically. Seeded a default `superadmin` account in `seed.ts`. Built dedicated vendor governance endpoints and pages: `/settings/taxonomy` for master data registers (vehicle classes, route line types, billing classifications), `/settings/branding` for platform primary color customization, semantic status tag theme mappings, and live UI preview, `/settings/system-health` for database latency, server telemetry, and maintenance mode emergency broadcast banner, and updated `UserManagementPage` with `SuperAdmin` role assignment and visual badges.)`, confirmed it comes back through the unauthenticated `GET /settings/public` (what the login page reads pre-auth) and that the file itself resolves at its `/uploads/...` URL with the correct content-type. `tsc --noEmit` clean, `vite build` clean.) · Previously (**Backend enforcement for module toggles**: the `Settings.enabledModules` toggle built for multi-client productization was frontend-only — `Sidebar.tsx`/`RequireModule.tsx` hid nav and blocked routes, but `requireModuleEnabled` (`middlewares/rbac.ts`) was applied to zero backend routes, so e.g. `GET /api/invoices` kept serving data with "Invoices" toggled off. `CLAUDE.md` requires both-layer enforcement; this closes the gap. **Fixed a fail-closed blocker found while wiring this up**: `getOrCreateSettings()` created the Settings row with no `enabledModules`, defaulting to `[]` — since `requireModuleEnabled` fails closed, any deployment where a trip was created before the seed ran would 403 every gated module at once. Now creates with `[...MODULE_KEYS]`. **`MODULE_KEYS` (`packages/shared-types`) drops from 8 to 6** — `locations` and `rate-cards` are no longer toggleable, moved to core: disabling them 403'd the trip wizard's location picker/rate lookup even though trip creation itself doesn't depend on the HTTP routes (locations are auto-created in-process by `resolveLocation()`, rate cards looked up via `findRateForLane()`). **Backend**: `requireModuleEnabled` applied to `invoiceRoutes`/`expenseRoutes`/`maintenanceRoutes`/`reportsRoutes`/`documentRoutes`/`folderRoutes`/`trashRoutes` (gating inside the router, not at the `index.ts` mount point, so it covers both the `/api` and root mounts of the same router instance); per-route gates added to the invoice-creation aliases living on `tripRoutes.ts` (`/trips/:id/mark-invoiced`/`unmark-invoiced`) and to `GET /mobile/documents`; `reportsController.ts`/`delayReportsController.ts` now null out (not zero — 0 would read as "confirmed zero" rather than "not tracked here") invoice/maintenance-derived fields in `getSummary`/`getFleetPerformance`/`getCustomReport`/delay analysis when their module is off, so the dashboard keeps loading instead of 403ing outright; `vehicleController.ts`'s `getVehicleFinancials`/`getFleetFinancials` (the "Vehicle P&L" endpoints, core nav with no toggle of their own) do the same for their invoice/maintenance-derived numbers; `trashController.ts` filters listed items and rejects restore/hard-delete by module (`RateCard` stays exempt — now core). New shared `getEnabledModules()` in `settingsController.ts` is the one source of truth `requireModuleEnabled` and every controller above reads from, so they can't drift. **Frontend**: `lib/api.ts`'s response interceptor now redirects home on a `MODULE_DISABLED` 403 (covers a tab left open on a gated page across a superadmin's live toggle — normal navigation was already blocked client-side); `reportsService.ts` types updated to reflect the now-nullable summary fields. **Found and fixed in passing**: `VehicleFinancialsPage.tsx`'s Excel/PDF export functions called `.toLocaleString()` directly on `net_profit`/`total_expenses`/`margin_percent` — would have thrown on `null` the moment maintenance is disabled; guarded with `?? 0`. Deliberately **not** touched: nested `invoices`/`maintenanceRecords`/`documents` includes on `getTripById`, vehicle/driver detail responses — stripping those touches core read paths for data the viewing company already owns, flagged as a non-goal rather than attempted. Also flagged, not fixed: `thirdPartyRoutes.ts` has no `authenticateJWT`/`authorizeRoles` at all — pre-existing hole, unrelated to modules. Verified end-to-end against a locally running API: disabling invoices 403'd `/api/invoices`, `/invoices` (root mount), `/api/reports/revenue`, and the trip mark-invoiced alias; `/api/reports/summary` stayed 200 with `revenue_this_month`/`monthly_revenue_chart` null; `/api/trips`, `/api/locations`, `/api/rate-cards` stayed 200 throughout; trash restore of a disabled-module type 403'd; re-enabling took effect on the very next request with the same still-valid JWT; deleting the Settings row and hitting a gated route recreated it with every module enabled rather than 403ing everything. `tsc --noEmit` clean on backend and frontend, `vite build` clean.) · Previously (**Custom Company Reports Generator (`/company-reports`)**: Replaced the broken `/reports/client-templates` prototype (which lost all fonts/fills/borders on export — `customTemplateExport.ts` copied cell styles from the sample data row *after* erasing that same row) with a byte-exact engine. New approach: `.xlsx` is unzipped, only the target worksheet's `<sheetData>` rows are spliced (values injected, style `s=` indices preserved from the cloned band row, formulas/merges/mergeCell/conditionalFormatting/dataValidation/dimension refs shifted by the row-count delta), and every other ZIP part (styles.xml, theme, drawings, charts, other sheets) is re-zipped byte-identical to the upload — verified by a unit test asserting exact byte equality against a real customer fixture (`docs/templates/COMPANY QUOTATION (1).xlsx`, which has JDL/IMILE/SHIPA/ARKAN/AKS sheets). Backend: `services/reports/xlsxTemplate/{splice,inspect,tripReportData,aliases,xmlRowSplice}.ts` (new deps `exceljs` read-only for inspection, `fflate` for the ZIP), new `ReportTemplate` Prisma model (file bytes + confirmed layout stored in Postgres, not the ephemeral `/tmp` uploads volume), new `/report-templates` routes (`inspect`, CRUD, `:id/preview`, `:id/generate`) gated by a new `company-reports` module key (with a seed backfill so the existing mercon.tech Settings row picks it up). Also fixed 3 underlying data bugs in `getCustomReport`/the new `fetchTripRows` service: date filtering on `createdAt` instead of the trip's own date, a hard 500-row cap, and `receiver` stubbed as `"Dropoff Stop N"` instead of the real `TripStop.location_name`. Frontend: new `CompanyReportsGeneratorPage.tsx` + `TemplateMappingEditor.tsx` (the human-confirms-the-mapping step the old page never had — `customMapping` existed in the engine but no UI ever passed it), `reportTemplateService.ts`. Wired as its own top-level sidebar item (not a Reports sub-tab) at `/company-reports`; `/reports/client-templates` redirects there. Removed `CompanyReportTemplatesPage.tsx` and `customTemplateExport.ts`. Hardened against two real customer files (a JDL trip-confirmation and an IMILE monthly invoice) which exposed four defects the synthetic fixture missed: (1) only the style-band row was replaced, so the customer's own sample rows survived underneath the generated data — `TemplateLayout` now carries an explicit, operator-editable `dataEndRow`; (2) shared formulas (`<f t="shared" si="N"/>`, which carry no formula text) cloned as empty cells, silently blanking the VAT/Total columns — formulas are now resolved against their master and emitted standalone; (3) a totals row's `SUM(M1:M37)` had its header-anchored start dragged down to `M14`, under-reporting the invoice total — row-shifting now distinguishes copy semantics (cloning, `$` pinned) from insert semantics (trailing rows, only refs at/below the block move); (4) the stale `calcChain.xml` cache was shipped pointing at moved cells, the usual cause of Excel's "we found a problem with some content" prompt — it is now dropped with its content-type override and relationship. Also fixed token substitution (banner tokens live in `sharedStrings.xml`, not inline in the sheet) and band detection (read a non-existent `style.id`). Verified: `npm run build` (root) succeeds; `npm run test:xlsx` (9 tests) passes; both real customer files generate 25-row reports that load cleanly in `openpyxl` (an implementation independent of the one used to read them) with **zero style mismatches** across every generated row, correct de-shared formulas, dates round-tripping as real dates, and UTF-8 (CJK) text intact; `prisma db push` + seed run twice against a local Postgres confirm the schema applies cleanly and the module-key backfill is idempotent; full API round-trip (login → inspect real fixture → create template → preview → generate) tested against a local server, output re-parses cleanly with ExcelJS. Added a rate-category filter (`Trip.rate_category`, e.g. "Trip", "Monthly Round", "Extra Trip/Round Trip") to preview/generate — real customer workbooks split trip-rate vs monthly-rate business into separate sheets (an IMILE file has an "extra" sheet), so a saved template needs to pull only its own slice rather than every trip for the customer. Verified end-to-end against a local Postgres: saved two independently-named templates ("IMILE Trip Monthly", "IMILE Extra") under the same customer with no collision, and each template's preview/generate correctly returns only its own rate-category slice of trips. · Same day, in parallel (**Smart Report Builder bugfixes**: post-review pass on the report builder found and fixed 3 defects — (1) `frontend/web-dashboard/src/router.tsx` referenced `moduleKey="locations"`/`"rate-cards"` on `<RequireModule>`, which no longer typecheck since a prior commit removed those two keys from `MODULE_KEYS` (they became always-on core infra) — **this broke `npm run build`**; fixed by dropping the two `RequireModule` wrappers to match the "core, not toggleable" intent. (2) `reportEngine/derived.ts`'s `tripIncome()` read `t.invoices[0]` unconditionally, but `reportEngine/resolver.ts` only includes the `invoices` relation when a spec explicitly references an `invoices.*` field — since `trips.revenue` is the default Values field in the Advanced Builder, every Quick Report preset, both landing-page templates, and the Ask Mercon parser, this crashed the query endpoint on the feature's own golden path; fixed by making `invoices` optional (`t.invoices?.[0]`). (3) `resolver.ts`'s row/column group-key join used a literal NUL byte as separator, making the file register as binary to tooling; replaced with `'||'`. **Not fixed in this pass** (flagged, needs an explicit decision): `requireModuleEnabled` was added to 7 previously-ungated route files (`reports`, `documents`, `folders`, `invoices`, `expenses`, `maintenance`, `trash`) in the same commit as the report builder work; `Settings.enabledModules` defaults to `[]` at the DB level and is only backfilled to the full list on **row creation** (`seed.ts`, `update: {}`) — if production's `Settings` singleton row already exists with an empty `enabledModules`, this 403s all 7 modules' APIs the moment it deploys. Needs a production data check (or an explicit backfill) before/after this lands, independent of the report builder itself. Built a complete, testable ad-hoc reporting system with a real backend Prisma query engine and drag-and-drop pivot table builder. **Backend**: `SavedReport` & `ScheduledReport` Prisma models (`npx prisma db push --accept-data-loss`), `reportEngine/schema.ts` (10 modules, canonical columns, join graph), `reportEngine/derived.ts` (shared `tripIncome`/`isEarned`), `reportEngine/resolver.ts` (`runReportQuery` with 400 injection guards, 1-hop relation include, JS group/agg/filter, KPI computation), `reportBuilderController.ts` & `reportBuilderRoutes.ts` with Zod validation (`reportQuerySpecBody`, `createSavedReportBody`, etc.) mounted at `/report-builder/*`. **Frontend**: `@dnd-kit/core` installed, `reportBuilderService.ts`, `askMerconParser.ts` (rule-based NL→spec parser), `ReportBuilderLandingPage.tsx` (top bar layout matching MERCON guidelines with scope pill `[ 🏢 MERCON Logistics ↕ ]` & category badge, 3 entry cards, 4 pre-built templates, Saved Reports & Scheduled Deliveries tabs), `QuickReportPage.tsx` (3-step wizard), `AdvancedBuilderPage.tsx` (3-panel workspace: expandable data panel, droppable structure zones for Rows/Columns/Values/Filters, auto-connect badge, FilterBuilder, live preview, KPI cards, Recharts view switcher: Table/Bar/Line/Pie/Area), `SaveReportModal.tsx`, `ScheduleReportModal.tsx`, CSV/Excel/PDF export. Mounted at `/report-builder` and registered in `Sidebar.tsx` under COMPLIANCE. Verified: backend `npx tsc --noEmit` clean, frontend `npm run build` clean.) · Previously (**Dedicated Company Excel Reports Page (`/reports/client-templates`)**: Built a dedicated page for generating customer-specific company Excel trip reports matching client format requirements (e.g. ARKAN, SHIPA, JDL). Features: Company Selector, Date Horizon & Status filters, Drag-and-Drop Excel Uploader, Presets Manager (save/load company templates locally), 4-Card Instrument Panel KPIs, Sheet Inspector (header row, sheet name, column auto-mapping), and Live Trip Ledger Preview table. Registered as `Company Formats` primary tab in `ReportsHeader.tsx` and routed at `/reports/client-templates` in `router.tsx`. Utility: `customTemplateExport.ts`; UI: `CompanyReportTemplatesPage.tsx`. Verified: `npm run build` succeeds cleanly with zero errors.) · Previously (**Multi-Client Productization Foundation** (owner decision: one codebase, one deployment per client, each on its own VPS/domain/database — see conversation for the full architecture writeup; not runtime multi-tenancy, no `tenantId` anywhere): **Backend** (`schema.prisma`, `seed.ts`): added `Settings` singleton model (`appName`/`companyLegalName`/`logoUrl`/`primaryColor`/`enabledModules`) and `User.isSuperAdmin` (a platform flag on top of `Admin`, NOT a 4th `Role` value — the 3-role rule is untouched); new `GET /settings/public` (unauthenticated, branding only), `GET/PUT /settings` (`requireSuperAdmin`, re-reads DB per request so revoking takes effect without re-login); new `requireModuleEnabled` middleware for future module-gated routes; `Trip.carrier_name` no longer defaults to the hardcoded literal `"MERCON LOGISTICS"`, now stamped from `Settings.companyLegalName` at create time (`getCompanyLegalName()`). **Removed `seedDefaultBillingLedgerData()`** from `prisma/seed.ts` — it seeded 6 fake customers + 10 fake trips/invoices on every container start, contradicting its own file header and `CLAUDE.md`'s seed rules; `backend/api-server/prisma/cleanup-fake-seed-data.sql` purges the rows it already left in Mercon's production DB (must be run manually, a redeploy won't do it). Fixed a real bug found in passing: `authService.ts`'s dev-only mock-login fallback had no `import.meta.env.DEV` guard, so a 5xx/network error in production silently granted a fake client-side Admin session. **Web dashboard**: de-branding codemod converted ~700 hardcoded `#E8450F`-family Tailwind arbitrary values (`bg-[#E8450F]` etc., 102 files) to the `bg-brand`/`text-brand`/... tokens already registered in `index.css`'s `@theme inline` block, plus the remaining on-screen literals (spinners, map pins, chart fills) to `var(--color-brand)`; new `useBranding`/`useApplyBranding` hooks fetch `/settings/public` and write it to the CSS vars + `document.title` at boot (login page included, pre-auth); new `BrandLogo` component replaces hardcoded `<img src="/mercon-logo.png">` in Sidebar/LoginPage with a Settings-driven `logoUrl` (falls back to the existing PNGs); `Sidebar.tsx` nav items gained an optional `moduleKey` filtered against `Settings.enabledModules`; new `RequireSuperAdmin`/`RequireModule` route guards (same pattern as existing `RequireRole`) applied to the 8 optional-module routes; `SettingsPage.tsx`'s previously-decorative "Company Details" tab is now a real superadmin-only branding/module-toggle form. Left deliberately out of scope: Excel export brand color (`utils/exportUtils.ts`/`exportInvoiceExcel.ts`) and the folder-color-picker palette (`CreateFolderModal.tsx`/`KpiCard.tsx`) — neither is app branding paint. **Mobile app** (`frontend/mobile-app/mercon-app`): `app.json` replaced with `app.config.ts` (`CLIENT_PROFILES` keyed by `APP_CLIENT` env, defaults to `mercon` so nothing changes for the existing build); `src/lib/api.ts` no longer falls back to `https://mercon.tech/api` on a misconfigured build — now reads the per-client `extra.apiUrl` from `app.config.ts` and throws if neither that nor `EXPO_PUBLIC_API_URL` is set; SecureStore keys (`TOKEN_KEY`/`SESSION_KEY`) namespaced by client slug so two clients' apps installed on one device can't collide (`auth-context.tsx`'s separate hardcoded `SESSION_KEY` literal removed in favor of importing api.ts's); brand color unified into `app.config.ts extra` and read at runtime by `src/theme/tokens.ts` via `expo-constants` (native can't re-theme live like web's CSS vars, so this is still build-time, just centralized); `tailwind.config.js` reads the same `APP_CLIENT` env at build time. **Deploy**: `docker-compose.yml` parameterized (`CLIENT_NAME`/`POSTGRES_DB`/port vars, all defaulting to today's Mercon values — verified with `docker compose config` that nothing changes for the existing deploy, and the `pgdata` volume was deliberately left unnamed rather than risk pointing at a different volume than Docker auto-generated for Mercon's live data); added `nginx/client.conf.template` for provisioning a new client's Nginx config without touching the live `nginx/mercon-api.conf`. **Explicitly not done in this pass** (flagged as follow-ups, not attempted blind): `.github/workflows/ci-cd.yml`'s tag-based/per-client-runner rework (recommended in the conversation, but it controls the only live production deploy and can't be tested here — needs the owner's review before it changes `git push` behavior); MTL's actual domain features (crane/subcontracted-vehicle tracking, "immediate trip" flow) — deliberately not designed without a scoping conversation with MTL, per Rule 0. Verified: backend `tsc --noEmit` clean, frontend `tsc --noEmit` + `vite build` clean, mobile `tsc --noEmit` clean, `docker compose config` resolves without error, seed re-run twice against local dev DB confirmed idempotent (no fake data, `isSuperAdmin` set once and not overwritten), full settings CRUD + superadmin-revocation-takes-effect-without-relogin tested end-to-end against a locally running API.) · Previously (**Expenses Module** (`/expenses`, new page): salaries, fuel, rent, and every other operating cost had nowhere to be recorded — MaintenanceRecord only covers vehicle service orders, and Invoice only covers customer billing, so there was no way to log money going out. **Database & Backend** (`schema.prisma`, `expenseController.ts`, `expenseRoutes.ts`, `trashController.ts`): added `Expense` model (free-text `category` e.g. Salary/Fuel/Rent, `status` Paid/Pending, optional `driverId`/`vehicleId` links, `amount`/`currency`/`expense_date`/`payee`/`payment_method`/`description`, standard ref_id + audit/soft-delete columns) with back-relations on `Driver`/`Vehicle`; registered `GET/POST /expenses`, `GET/PATCH/DELETE /expenses/:id` gated to Admin/Operator via `authorizeRoles`; wired into the recycle bin (restore/hard-delete). **Shared Types** (`packages/shared-types`): added `EXPENSE_CATEGORIES`/`EXPENSE_PAYMENT_METHODS` constants (free-text columns, not Postgres enums, same pattern as `VEHICLE_TYPES`). **Frontend Web Dashboard**: `expenseService.ts`, `ExpenseListPage.tsx` (KPI cards for total/paid/pending/salary amounts, filterable/searchable `DataTable` ledger, CSV export), `ExpenseModal.tsx` (create/edit form with category, status, optional driver/vehicle link, amount, payment method, payee, notes); added to the sidebar FINANCE group and the router at `/expenses`. No seed data added (owner rule: never seed fake financial records). Verified: `tsc --noEmit` clean on both backend and frontend, `vite build` succeeded; local schema push to the dev Postgres could not be verified in this session because Docker/local Postgres was not running — run `npx prisma db push` (or redeploy) before relying on the `Expense` table.) · Previously (**Savable & Selectable Workshops and Work Done Service Details in Maintenance Module**: Operators can now save workshops and service details (e.g. "Tire Puncture Repair", "Oil & Filter Change", "Brake Pad Replacement") so they are selectable directly in **Add Maintenance Record** instead of typing from scratch every time. **Database & Backend** (`schema.prisma`, `seed.ts`, `maintenanceController.ts`, `maintenanceRoutes.ts`): Added `SavedWorkshop` and `SavedWorkDone` models with idempotent seeding for default workshops and common service details. Updated `getWorkshops` to merge saved entries with historical orders; added API routes for `POST /workshops`, `DELETE /workshops/:id`, `GET /work-items`, `POST /work-items`, `DELETE /work-items/:id`. **Frontend Web Dashboard** (`frontend/web-dashboard`): `WorkshopField.tsx` upgraded to a selectable dropdown showing saved workshops with an inline `+ Save New Workshop` popover. `WorkDoneSelect.tsx` added to render multi-item selectable service chips with an inline `+ Save New Service Item` dialog. `ManageWorkshopsModal.tsx` added to let operators manage saved workshops and service details anytime from the top bar of the Maintenance list page. Verified clean build: `tsc` and `vite build` succeeded with zero errors.) · Previously (**Monthly Trips by Company** (`/trips/monthly`, new page): monthly-contract work ("this company gets N trips this month; assign a driver and a truck per day") was invisible as a plan — the trip ledger is flat, paginated 20 at a time and sorted by status, so answering "who is covering ARKAN on the 14th" meant scrolling and regrouping in your head, and "is every day of this month covered" was unanswerable. **Backend** (`tripController.getMonthlyTripBoard`, `GET /trips/monthly`, registered before `/:id` so "monthly" isn't read as a trip id — no schema change): returns a whole month in one response, already grouped customer → day, with per-company driver/vehicle/category rosters and a month summary (trips, companies, distinct drivers, distinct vehicles, value, trips still missing a driver or truck, status breakdown). A trip's day is `planned_start` falling back to `createdAt` — the same rule `getTrips`' date filter already uses, so the ledger and the board can never disagree about which month a trip is in; a trip landed on its created date is flagged `date_is_inferred` and says so in the UI rather than pretending it was scheduled. Filtering by rate category / vehicle type matches **either** the trip's own copied column **or** its rate card's, because trips created before `Trip.vehicle_type`/`rate_category` existed only carry the tier on the card — matching only the trip column would have silently hidden them from a "Monthly Round" filter. Money shown is `billing_amount ?? trip_charges`, never invented: a trip with neither contributes 0 and renders as unpriced. Capped at 5000 trips with a `truncated` flag the page surfaces, so a runaway month is visible rather than silently short. **Frontend**: `MonthlyTripsPage.tsx` (month stepper + `This month` reset, filters for company/rate category/vehicle type/status/search, Excel export of the flattened month, honest empty states) and `components/trips/monthly/` — `MonthlyCompanyCard` is one card per company (name, trip count, an amber "N to assign" badge, total value) holding a vertical stack of that company's trips: each trip is its own compact card with a date/status row and a labeled 2-column field grid (driver, vehicle, route, amount, category), long driver names and routes truncated with a hover title instead of stretching the card. Clicking a trip opens `TripDetailDialog` on the board (full field breakdown + an "Open full trip" button to `/trips/:id`) rather than navigating away immediately. **Revised six times, same day, on owner feedback**: pass 1 built a page-level 6-tile KPI strip and, per company, a Calendar/Schedule toggle — replaced with a per-company flat table per the owner's "just a table, date/driver/vehicle" ask. Pass 2: the table's wide horizontal rows were still awkward for long names/routes, and the owner separately flagged the per-company colour system (`companyHue`, a company-name-hashed pick from 7 hues for the avatar chip and accent rail) as generic, "slop kinda UI" — replaced the table with the vertically stacked trip cards described above (limited-width truncation instead of unbounded flex growth) and removed `companyHue`/`COMPANY_HUES` entirely in favour of one consistent brand-orange avatar for every company. `daysInMonth`/`dayKey`/`isToday`/`STATUS_DOT` (calendar-only helpers) and the KPI tile component were deleted from pass 1's code, not left dead. Companies still sort busiest-first. Pass 3: pass 2's vertical field-grid trip cards (a date/status row plus a 2-column labeled grid) still produced far more scrolling than the data needed and were not what "vertically stacked" meant — the owner wanted the *company cards* stacked vertically down the page (already true) each holding one **tight table**, and a click on a row (not the whole card) opening the detail dialog. `MonthlyCompanyCard` now renders a compact `Table` again — tighter row padding than pass 1's table, driver/route/category cells capped with `max-w` + `truncate` + a hover title instead of letting a long name or route stretch the row — with `TripDetailDialog` reused unchanged from pass 2 on row click. Pass 4: the owner's "vertically stacked" turned out to mean company cards placed side by side (each column reading top-to-bottom is "vertical"), not the single full-width column pass 3 built — confirmed with `AskUserQuestion` before touching code, given three prior misreads of the same phrase. `MonthlyTripsPage`'s company list is now `grid grid-cols-1 lg:grid-cols-2 gap-4 items-start` (two per row on desktop, one on mobile, `items-start` so a company with fewer trips doesn't stretch to match a taller neighbour) instead of a single vertical stack — cuts the up-down scrolling a full-width column forced once there were more than a couple of companies. Pass 5: trimmed the table itself from 7 columns to 4 — Date, Driver, Vehicle, Status — on the owner's ask not to clutter the row with lower-priority fields; Route, Category and Amount are still there, just moved into `TripDetailDialog` (already the one-click destination since pass 2) instead of the row. Pass 6 ("overall page UI/UX upgrade"): the page was three detached white boxes stacked with `gap-5` — a header strip, a search card, a filter card — which read as three unrelated things rather than one control surface for one list. They are now a single **command bar** card: row 1 is the icon badge + title + subtitle on the left and, on the right, a segmented `MonthStepper` (‹ August 2026 › with a formatted text label and a transparent native `input[type=month]` overlaid so clicking still opens the platform month picker, plus a "Today" segment that appears only off the current month), an icon-only refresh/export pair grouped in one bordered container, and the single primary `New Trip` button. Row 2 joins the search box and all four filter `Select`s into **one** segmented control — one border, internal hairline dividers (`divide-x`), borderless children — instead of five separately-bordered pills. Row 3 is a result-context line (`August 2026 · 8 companies · 124 trips · 6 need assignment`, the last in amber only when non-zero) paired with **removable filter chips** (each applied filter as a chip with its own ×, plus "Clear all"), replacing the previous bare "Clear 3" button that never said *what* was applied. `appliedFilters` is derived once so the chip row and the clear-all count cannot disagree. Company cards were retuned to match: header is now avatar + name + a phone/total meta line on the left with the trip count and unassigned badge right-aligned (previously all crammed inline after the name), table headers dropped to `h-8`/`text-[10px]`, status right-aligned, and the detail dialog restructured into a tinted "Scheduled" block + labelled `Detail` pairs on hairline-separated rows. Page padding `pb-6`→`pb-8`, section rhythm `gap-5`→`gap-4`. Rate category / vehicle type options come from `RATE_CATEGORIES`/`VEHICLE_TYPES` in `@mercon/shared-types`, not a local list. Sidebar entry added under OPERATIONS next to Trips (`/trips`'s active-state rule updated so it no longer also highlights for `/trips/monthly`). **Not exercised in a browser** — no local Postgres or Docker in this environment; verified by `tsc --noEmit` clean on api-server and `tsc -b` + `vite build` clean on web-dashboard. Also **repaired PROGRESS.md itself**: this file had committed merge-conflict markers (`=======` / `>>>>>>> Stashed changes`) around two rival "Last updated" lines since `3e2dcd1`; kept the newer 2026-08-12 chain and spliced the 2026-08-11 tonnage-tier entry — which existed only on the stashed line — back into it so no history was lost)) · Previously (**Added Vehicle Type / Rate Category filters to the Rate Cards ledger**: `RateCard.vehicle_type`/`rate_category` were already stored and imported per-tier, but the `/rate-cards` list page only let you filter by status/search — there was no way to narrow the ledger down to just "Monthly Round" or just "13.5M-20TON" rows even though those are exactly the dimensions that distinguish otherwise-identical lanes. Added `vehicle_type`/`rate_category` as real server-side query params on `GET /rate-cards` (`rateCardController.getRateCards`, additive `whereClause` filters, same pattern as the existing status filter), wired two new dropdowns (populated from the canonical `VEHICLE_TYPES`/`RATE_CATEGORIES` lists in `@mercon/shared-types`) into `RateCardListPage.tsx`'s ledger toolbar next to Status, and extended the active-filter banner / "Show All Rates" reset to cover all three filters together. No schema or architecture change — the fields already existed, this only exposes them as filters) · Previously (**Tonnage tier / trip-type ("6 ton", "10 ton", "one trip", "two-way/round trip") now actually drives rate-card matching, instead of being decorative labels**: owner reported these weren't "properly connected." Root cause: `RateCard.vehicle_type`/`rate_category` were free-text columns nothing read at lookup time — `findRateForLane` (the one shared rule used by `GET /rate-cards/lookup`, `createTrip`'s auto-fill, and invoicing's fallback) filtered only by customer+lane and broke ties with `orderBy: updatedAt desc`, so a lane priced at both "6 TON" and "10 TON" resolved to whichever card was touched most recently, ignoring tonnage/trip-type entirely; `Trip` itself had no field to record which tier was actually dispatched; and the manual create/edit rate-card forms (`RateCardFormDialog`, `CreateRateCardPage`, `EditRateCardPage`) never exposed these two fields at all — they were only ever populated via CSV bulk import. **Not converted to a hard Postgres enum** (deliberate, discussed with owner): live DB access wasn't available to confirm every existing row matches a known list, and `prisma db push --accept-data-loss` runs automatically on every deploy — a strict enum could silently drop an unlisted legacy value. Instead, `VEHICLE_TYPES`/`RATE_CATEGORIES` (`packages/shared-types/src/index.ts`) are the 10/9 real values found in the 652-row import workbook (`docs/templates/MERCON_RateCards_ReadyToImport_Quotations.xlsx`), validated at the application layer only (Zod in the backend, dropdowns in the UI); the DB columns stay nullable `String`. **Schema** (additive, non-breaking): `Trip.vehicle_type`/`Trip.rate_category` (nullable strings) — copied from whichever rate card was actually applied (or typed by hand for a one-off price), so the dispatcher's tier selection survives even if that rate card is later edited, and trip-level reporting on tier becomes possible. **Backend**: `findRateForLane` (`services/rateLookup.ts`) now accepts optional `vehicleType`/`rateCategory` — `undefined` means "any tier" (old behavior, for callers that don't know), `null` means "only tier-less cards", a string filters exactly; wired through `GET /rate-cards/lookup` (query params), `createTrip`'s fallback matching, and `tripLifecycle.completeTripAndInvoice`'s legacy-trip fallback (only constrained when the older trip actually recorded a tier, so it doesn't wrongly narrow pre-existing trips to tier-less-only). `createTripBody`/`rateCardController` (`createRateCard`/`updateRateCard`/`bulkImportRateCards`) now validate `vehicle_type`/`rate_category` against the shared list via new `vehicleTypeField`/`rateCategoryField` Zod schemas, rejecting unknown values row-by-row on import rather than silently storing typos. **Frontend**: dropdowns (backed by the same shared list) added to `RateCardFormDialog`, `CreateRateCardPage`, `EditRateCardPage` — previously read-only badges with no way to set them by hand. Trip creation (`CreateTripPage`, `CreateTripModal`/`TripStepRatesBilling`) gained a tier selector inside the existing "new lane, no rate yet" flow, so a fresh rate card (or one-off price) created mid-dispatch can be tagged with a tier instead of always saving `null`; the existing "N rates for this lane, pick one" card picker (already tier-aware via badges) is unchanged. Verified: `tsc --noEmit` clean on `backend/api-server`, `tsc --noEmit` clean on `frontend/web-dashboard`. **Not exercised in a running browser or against a live DB this session** — no local Postgres connection was available; the enum-vs-validation decision and the workbook-derived value list are the two things to double check against the live database before/soon after this deploys.) · Previously (**Upgraded Top Customers on the Ilan Dashboard from a bare rank+bar list to real per-customer cards**: found that `reportsController.ts`'s `getRevenueReport` already computed the customer's `id` internally (via `customer.findMany({select:{id,name}})`) but never returned it — added `id` to the `top_customers` response (1-line, additive, verified only 2 other pages consume this endpoint and neither reads more than `name`/`value`, so no breaking change). That unlocked: rows now navigate to `/customers/:id` on click, show a real credit-exposure bar (`CreditExposureKpi` — previously built but unused anywhere — driven by `Customer.credit_limit` and that customer's real `Pending`/`Overdue` invoice total, reusing the same `invoicesRes` array already fetched for Actions Needed rather than firing new requests) omitted entirely when a customer has no credit limit set (never faked), and an "N active" trip chip from `customer.trips` when any are in an in-flight status. Avatars use the same initials-chip pattern as `UserChip.tsx`, colored from the same 6-hue palette introduced for Actions Needed's categories so the two flagship widgets read as one visual system. `AdminDashboardPage.tsx` gained one new query (`Promise.all` over `customerService.getById` for the top 5 ids). `TopCustomersWidget.tsx` rewritten; `reportsService.ts` type updated. · Previously (**Gave each Actions Needed category its own color, not just its icon**: rows now carry a subtle full-row background wash and a left accent stripe in the category's color (amber/blue/rose/purple/indigo/teal), and the active filter chip tints to match instead of a plain white/gray surface. Locations moved off slate onto teal so all 6 categories read as distinct colors, not "5 colors + 1 gray." Severity pills (rose/amber/slate) and the brand-orange hero count are untouched — this only adds a second, category-level color dimension on top of the existing severity one. `components/dashboard/ActionsNeededWidget.tsx` only. · Previously (**Rebuilt Actions Needed into an "Action Center" on the Ilan (Admin) Dashboard**: was a 2-category (documents expiring, maintenance due) split-panel card; now a unified, urgency-sorted list across 6 real categories with filter chips (All / Documents / Maintenance / Delays / Invoices / Rate Cards / Locations), a brand-orange hero count + severity breakdown, and a "You're all caught up" empty state. Four new categories, each reusing an existing predicate/endpoint rather than inventing one: **delay stops needing a reason** (`reportsService.getDelayLog({ needs_reason: 'true' })` — server-computed, previously shown read-only only on the full Delay Report page), **overdue invoices** (same `due_date < now && status !== 'Paid'` check `InvoiceListPage.tsx` already uses), **rate cards not linked to a lane** and **locations missing an address** (same predicates `RateCardListPage.tsx`/`LocationListPage.tsx` already compute). Explicitly left out: unsettled post-trip charges (already its own widget/KPI card on this page — would've duplicated), driver risk-score/stuck-in-Draft/stuck-in-Maintenance signals (no existing threshold convention to anchor a number to), ICCES device staleness (`Vehicle.last_seen_at` is never read anywhere in the frontend today, unverified as populated), payment-approval-pending (no server-side filter, needs backend work), and snooze/dismiss (confirmed nowhere in the codebase — resolving means navigating to the real page, or for delays, a small inline action). That last case needed one: `logStopDelay` and the `DelayReason` types were fully built server-side but **never called from any frontend component** — the Delay Report page only showed `needs_reason` as a read-only badge. Added `components/trips/LogDelayReasonModal.tsx` (mirrors `PostTripSettlementModal`'s click-row→modal→submit pattern) so this is now a genuinely resolvable action instead of a dead end. `AdminDashboardPage.tsx` gained 4 new queries (delay log, invoices, rate cards, locations) and the widget moved to its own full-width row (Top Customers also promoted to its own row) instead of squeezed into a shared half-width card. · Previously (**Gave the Ilan (Admin) Dashboard's Settlement Queue and Revenue KPI cards their own footer instead of a reskinned truck**: the animated route/truck footer (from `/vehicles`) fits Active Trips and Fleet Available — both literally about vehicles — but Settlement Queue and Revenue aren't vehicle concepts, so recoloring the same truck animation for them was decoration with no meaning. Settlement Queue now shows a real aging distribution (`KpiCard`'s native `progressSegments`) bucketing pending trips by days-since-completion (≤1d / 2-3d / 4d+, amber→rose), computed from `unsettledTrips` client-side. Revenue now shows a real 6-month sparkline (`KpiCard`'s native `chartData`) from `summary.monthly_revenue_chart`, replacing the truck footer — also less code, since both are `KpiCard` features already built for this rather than new custom SVG. Active Trips and Fleet Available keep the truck/route `KpiRouteFooter`. `AdminDashboardPage.tsx` only. · Previously (**Redesigned the Admin Dashboard header row**: replaced the bare title-and-button strip with one matching the main `/` Dashboard's own header convention — a tinted icon badge, a 2xl title with an "Admin Only" pill (this page is unlisted from the sidebar, worth signposting), a subtitle, a live "Updated Xm ago" freshness label driven by `useIsFetching({ queryKey: ['admin-dashboard'] })`, and the same pill-shaped "Refresh All" button style as the main dashboard (was a mismatched `Btn` "secondary" pill before). The hand-rolled inline success banner (which pushed the KPI strip down whenever it appeared) was replaced with the main dashboard's fixed bottom-right slide-in toast instead, so header spacing stays stable regardless of toast state. `AdminDashboardPage.tsx` only — no other page touched. · Previously (**Moved the Admin Dashboard off the sidebar to a direct-link-only route** (`/admin-dashboard` → `/Ilan`), per owner request, and restyled its KPI strip to match `/vehicles`'s instrument-panel cards — uppercase title, count+unit-label value, `trend`/`trendValue` chip, a `kpi-icons.tsx` line icon (not lucide), and the same animated "route map" `customFooter` (grid + street-line network + dashed route + pulsing 3D truck), now extracted into a shared `components/ui/KpiRouteFooter.tsx` instead of hand-duplicated per card. Unlike the Vehicles page, these cards are informational only (no `isActive`/`onClick` filter toggle — nothing on this page to filter into). `Sidebar.tsx`'s Admin-only entry was removed (page keeps `RequireRole roles={['Admin']}`, just unlisted). · Previously (**Added the Admin Dashboard** (`/admin-dashboard`, Admin-only): a new, separate page — the existing `/` dashboard is untouched — built from the owner's prioritized widget list. **Active Trips** (`ActiveTripsWidget`) shows a live in-flight count/list via `tripService.getAll({ status: 'Dispatched,AtPickup,InTransit,AtDelivery' })`, deliberately not the "total trips" KPI already on the main dashboard. **Post-Trip Settlement Queue** (`LaborChargeQueueWidget`) consolidates the main dashboard's three scattered unsettled-trip UI patterns (banner + insights alert + ledger card) into one `DataTable` with an "Enter Labor Charges" action per row, still driving the existing `PostTripSettlementModal`/`tripService.getUnsettled`/`updateFinancials` — no lifecycle change: there is no "Delivered" status, `AtDelivery→Completed` still happens automatically from the driver's mobile "Complete Delivery" action, and `is_post_trip_settled` is still a separate flag, not a gate. **Top Customers** (`TopCustomersWidget`) reads `reportsService.getRevenueReport().top_customers`. **Actions Needed** (`ActionsNeededWidget`) merges expiring documents (`documentService.getAll({ expiring_within_days: 30 })`) and maintenance due soon (`maintenanceService.getAll` filtered/sorted by `next_service_due`) into one triage panel, reusing the previously-unused `ExpiryCountdownKpi`. A small KPI strip (fleet available/on-trip, revenue trend) reuses `KpiCard`/`CustomKpiWidgets`. Gated with `RequireRole roles={['Admin']}` + an Admin-only sidebar entry (`Sidebar.tsx`), same pattern as `/settings/users`. No backend or schema changes — every widget calls existing service functions. · Previously (**Removed the simulated "Driver live dispatch chat" widget from the web dashboard**: the WhatsApp-style chat panel on the Dashboard (Chats/Updates/Calls tabs, the in-memory driver message simulation, the "Send Mobile Dispatch Message" modal, and the "Contact Driver" button on Active Transit Status) was mocked UI with no backend — no real-time driver messaging exists in Mercon. Removed the whole component along with its dead state/handlers (messages, activeChatDriverName, chat send/reply simulation, drivers-for-messaging query) from `frontend/web-dashboard/src/pages/dashboard/DashboardPage.tsx`; the Labor Charges Ledger card now spans the full row in its place. No backend or schema changes. · Previously (**Search normalisation across every ledger and search bar**: searching only worked if you typed the value exactly the way it was stored — one whole-string `contains` per field. Typing a full name found nothing (the driver roster only searched `first_name`), word order mattered, and a plate or reference typed without its dash ("ksa1029", "drv0001") matched nothing. Search is now normalised in one place per side: `backend/api-server/src/utils/search.ts` (`buildSearchAnd`) builds the Prisma filter for the server-paginated ledgers (trips, drivers, vehicles, customers, invoices, maintenance, locations), and `lib/search.ts` in the web dashboard and the mobile app (`matchesSearch`) filters the client-side lists. The query is split into words; every word must match at least one field (AND across words, OR across fields), so word order stops mattering and "john riyadh" works. Case is ignored, and punctuation is bridged both ways — on the client by comparing separator-stripped text, on the server by also splitting a word at letter/digit boundaries and requiring each part in the same field. The client copies additionally fold accents and Arabic-Indic digits. Field coverage was filled in where it was plainly missing: drivers now search last name, ref id, phone, licence and assigned plate; customers search the contact phone; invoices search the trip ref; locations search the address. Verified against the local database: "drv0001" → DRV-1008's roster row, "najjar sami" → Sami Al-Najjar, "ksa1029" → KSA-1029, "trp 3009" → TRP-3009. Known gap: on server-filtered ledgers a *word* typed without its punctuation ("alnajjar" for "Al-Najjar") still misses, because Postgres can't strip the separator out of the stored value through Prisma; the client-side lists handle that case. No schema or API-shape changes.) · Previously (**Rate card import**: `RateCard` schema gained `vehicle_type`/`rate_category`/`via_location` — free text, not enums, since each customer's quotation names its own vehicle tiers ("5 TON", "DYNA 3 TON", "13.5M-20TON"). New `POST /rate-cards/import` (flat one-row-per-lane-×-vehicle-type sheet: Customer/Rate Category/Origin/Via/Destination/Vehicle Type/Price/Currency, mirrors `bulkImportCustomers`) wired to an "Import Excel" button on the Rate Cards list via the shared `ExcelImportDialog`; the customer must already exist (matched by name — a rate card import can't create one, `Customer.contact_phone` is required and isn't on this sheet); rows with `rate_category` "Surcharge" skip lane resolution entirely for flat fees (labour charge, per-stop fee) that aren't an origin→destination lane. Blank template at `frontend/web-dashboard/public/templates/MERCON_RateCards_Import_Template.xlsx`; the owner's actual 7-carrier quotation workbook (`docs/templates/COMPANY QUOTATION (1).xlsx` — JDL/IMILE/SHIPA/RTL/HORIZON/ARKAN BARWAN/AKS, each becoming a Customer) was converted to a ready-to-import flat file at `docs/templates/MERCON_RateCards_ReadyToImport_Quotations.xlsx` (652 rows) for the owner to sanity-check before it's actually imported — not yet imported. Schema change not yet pushed to any database (needs `prisma db push`, which happens automatically on next deploy).) · Previously (**Web dashboard build unblocked, list toolbars unified, mobile safe areas fixed**: the web dashboard build was failing on `main` — the deprecated `ScheduleMaintenanceModal` shim re-exported a name (`MaintenanceRecordModal`) that `MaintenanceRecordModal.tsx` never exports as a named export (it is the default export, plus a `ScheduleMaintenanceModal` alias), so `tsc -b` failed with TS2724 and the CI/CD deploy could not build the image; the shim now re-exports the default together with the existing `ScheduleMaintenanceModal` alias. Documents Center and the Invoices ledger each had their own toolbar row holding a search box and a status filter above the table, duplicating what `DataTable` already renders — both now pass their filter through `DataTable`'s `filterElement` and the standalone toolbars are gone (Documents keeps a grid-mode-only bar for bulk actions). On mobile, every screen switched from React Native's built-in `SafeAreaView` (which only insets on iOS) to the `react-native-safe-area-context` version, so Android screens no longer render under the status bar; the operator More screen shows the signed-in role instead of a hardcoded "Operator", and vehicle renewals whose vehicle was deleted are dropped instead of listing as "Unknown Vehicle". The desktop sidebar rail toggle also moved out of the header (where it shifted the header contents on every toggle) onto the sidebar's own right edge, vertically centred, so it stays put whether the rail is expanded or collapsed — superseding the "Menu" button described in the entry below. It is a 28px circle carrying a double chevron (a panel action, not a back arrow) with a 44px click target, and ⌘/Ctrl+B toggles it as well. No schema or API changes.) · Previously (**Maintenance details page rebuilt as a unified dashboard**: `/maintenance/:id` had a header, a separate lifecycle rail, a 4-tile metrics row, a work-report card, two side cards (Vehicle, Workshop) and a full-width Invoice card stacked below — six blocks pushing billing off-screen. Header, lifecycle rail and the four metrics are now one hero card with an integrated stat strip and a brand accent rail; the work report and billing/invoice sections are joined into one tabbed card ("Work Report" / "Billing & Invoice", with an invoice-count badge) so billing no longer needs a second scroll; Vehicle and Workshop are joined into one "Service Provider" card with divided sections. Borders/shadows moved to the same softer layered style used on the driver details page. No data or API changes.) · Previously (**Driver details page rebuilt as a modern dashboard**: `/drivers/:id` was four stacked blocks (header card, a 4-tile KPI row, scheduled-days card, trips table, documents card, plus two small right-column cards) that pushed the real content below the fold. Identity, actions and the four stats are now one hero card with an integrated stat strip and a brand accent rail; the icon-only actions (refresh/export/vault/delete) are a single segmented control. Scheduled days and dispatch history are joined into one tabbed "Trip Operations" card with counts on each tab and internal scroll instead of page scroll, and Credentials + Assigned Vehicle + Documents are joined into one right-hand "Driver Profile" card with divided sections. A license-expiry banner (expired / ≤ 30 days) appears only when it applies, the assigned plate is a chip that opens the vehicle, and borders/shadows moved to a softer layered style. No data or API changes.) · Previously (**Desktop sidebar collapses to an icon rail**: the dashboard sidebar can now be shrunk with a "Menu" button in the header, next to Back — it animates between 220px and a 76px icon-only rail (labels, group headings and the user block fade out; unread counts become a dot; every item keeps a tooltip), the main content reflows with it, and the choice is remembered in `localStorage` across pages and reloads. Rail mode is desktop-only (lg and up); the mobile off-canvas drawer is untouched.) · Previously (**Map tracking icons upgraded to transparent 3D assets**: replaced generic map markers with transparent 3D orange/green warehouses and 3D truck, removing boxy backgrounds and dark borders per user feedback) · Previously (**Workshops are remembered and offered for reuse**: every service-order form asked for the workshop name as a blank text box, so the same workshop got retyped — and mistyped into near-duplicates — on every order. `GET /api/maintenance/workshops` now returns the distinct workshops already used (case-insensitive, newest spelling and contact win, with an order count), derived from the service orders themselves — no Workshop table, no schema change. `components/fleet/WorkshopField.tsx` replaces the plain input everywhere a workshop is entered (Send to Workshop dialog, maintenance create/edit form, maintenance details edit, vehicle details Log Maintenance): it stays free text, but suggests saved workshops as you type and fills in the workshop's contact number when you pick one. A workshop typed once is offered from then on; the `workshops` query is invalidated after every service-order write.) · Previously (**"Mark Maintenance" now opens a real service order**: the Vehicles page wrote `Vehicle.status = 'Maintenance'` directly, so a vehicle could sit in the workshop with nothing on the Maintenance page to show for it — and no order to later mark completed. The workshop state is now owned by the service order end to end. `SendToWorkshopDialog` (`components/fleet/SendToWorkshopDialog.tsx`) collects workshop, type, odometer and reason and creates an `In_Progress` record; the API's `syncVehicleMaintenanceStatus` moves the vehicle to Maintenance off the back of it. The reverse is `POST /api/maintenance/vehicles/:vehicleId/return-to-service`, which closes every open order (Completed, `end_date` now) and releases the vehicle. Every entry point routes through those two: the row actions, the status-badge quick dropdown, the bulk actions, and the details-page banner and dropdown. On the vehicles list the workshop toggle is now a one-click button in the Actions column ("To workshop" / "Return to service") instead of being buried in the ⋮ menu, which also gained a "Service History" link — `/maintenance?vehicle=<plate>` pre-fills the maintenance search. Bulk "Mark Maintenance"/"Mark Available" became "Send to Workshop"/"Return to Service".) · Previously (**Vehicles stopped getting stuck in "Maintenance"**: a vehicle only left `Vehicle.status = 'Maintenance'` when a service order was *edited* from an open state to Completed/Cancelled. Every other path left it in the workshop forever — logging an already-completed order (the Log Maintenance modal on the vehicle details page defaults to Completed), deleting the open order, or moving an order to another vehicle — so the Vehicles list, its Maintenance Bay KPI card and the vehicle details badge all kept reading `Maintenance`. Vehicle status is now re-derived in one place, `utils/vehicleMaintenanceStatus.ts` → `syncVehicleMaintenanceStatus(vehicleId)`: any open order puts the vehicle in `Maintenance`, none releases it back to `Available` (a vehicle that is `OnTrip`/`Inactive` is never touched). It is called from create, update and delete in `maintenanceController`. The legacy `In Progress` (space) spelling now counts as open everywhere, including the list filter. Existing production data is repaired by an idempotent seed step, `releaseVehiclesStuckInMaintenance()`, which normalises `In Progress` → `In_Progress` and releases vehicles that have service history, nothing open, and have not been touched since their last order changed — that last condition is what stops it clobbering a deliberate "Mark Maintenance" from the Vehicles page. The maintenance list and details pages now also invalidate the vehicle caches so the change shows without a reload.) · Previously (**Create Trip step 3 re-laid out around the two stops**: the step stacked three full-width blocks — a pricing-lane box, then an "exact pickup" block and an "exact dropoff" block that each carried a permanently-open 220px Leaflet map plus four separate inputs (address search, stop name, address textarea, plus the hub combobox in a different section). The schedule and the price sat far below the fold and one stop's fields were split across two places. Step 3 is now a two-column grid of `TripStopCard` (`components/trips/TripStopCard.tsx`), one card per stop holding everything about it in the order a dispatcher thinks: pricing hub → exact point → planned time. `LocationPickerMap` gained a `compact` mode where the address search is the only field on screen; what the driver will receive (name, address, coordinates) is shown as a read-only summary line, and the map ("Adjust pin") and the manual name/address inputs ("Edit") fold open only when needed — the name inputs auto-open while the required name is still blank. Time presets trimmed from 9 chips to 7 shorter ones, the datetime picker's own preset popover already covers the rest. No behaviour, payload or validation change; the rate lookup, the pin-far-from-hub warning and the centroid-does-not-overwrite-a-searched-pin guard are unchanged. `LocationPickerMap`'s hardcoded light-mode hex colours were also swapped for theme tokens, which fixes its dark mode on the trip details stop editor too.) · Previously (**Vehicle P&L page rebuilt as a fleet profitability dashboard**: `/vehicles/financials` could only show one truck at a time — the owner could not see which vehicle earns the most or loses the most without clicking through every plate, and the page had no charts at all. The blocker was the API: `GET /vehicles/:id/financials` returns a single vehicle, so ranking the fleet was impossible client-side without one request per truck. **Backend** (`vehicleController.ts`, `vehicleRoutes.ts`, no schema change): new `GET /vehicles/financials/fleet` aggregates every non-deleted vehicle in three queries (vehicles, trips+invoices, maintenance) and returns one P&L row per vehicle — income, expenses, maintenance/renewal split, net profit, margin %, trip count — plus a `fleet_summary` (totals, profitable/loss-making/idle counts) and a fleet-wide `monthly` income/expense/profit series. The route is registered before `/:id` so the literal path is never captured as an id. The revenue rule that was inline in `getVehicleFinancials` is now the shared `tripIncome()` helper (`billing_amount` → invoice `total_amount` → `trip_charges`, counted only for `Completed`/`Invoiced`), so the fleet and per-vehicle reports can never drift. Both endpoints now accept optional `from`/`to` ISO bounds and both return a `monthly` series. **Frontend**: `VehicleFinancialsPage` is now two `Tabs` — *Fleet Comparison* (default at `/vehicles/financials`) and *Vehicle Detail* (default at `/vehicles/:id/financials`), with the tab switch rewriting the URL so both existing routes keep working. Fleet tab: 4 KPI cards, a **diverging horizontal bar chart** of net profit per vehicle (green/red, click a bar to drill in, capped at the 15 most significant), an expense-composition **donut** (maintenance vs renewals), a **stacked area** fleet income-vs-expense trend, *Most Profitable* / *Biggest Loss Makers* leaderboards with proportion bars, and a sortable full-fleet `DataTable` (sort by income/expenses/net/margin/trips; row click drills in). Vehicle tab: the plate `Select` became a searchable `Combobox` (a 100-item plain select was unusable), plus a `ComposedChart` of monthly income/expense bars with a profit line, a revenue-to-net breakdown bar, and the two itemized ledgers rebuilt as real `Table`s inside `ScrollArea` with totals footers. A `ToggleGroup` period preset (3M/6M/12M/All) drives the `from` bound on both tabs; Export Excel is tab-aware (fleet comparison sheet vs per-vehicle statement). New shadcn primitives scaffolded on the already-installed Base UI deps: `ui/scroll-area.tsx`, `ui/toggle-group.tsx`; `DataTable`'s `Column.header` widened from `string` to `React.ReactNode` so headers can carry sort buttons. Charts use the existing `ui/chart.tsx` + recharts — no new packages. **Not verified in a browser**: the local Postgres rejects the `mercon_user` credentials in `backend/api-server/.env` (`denied access on the database mercon_db.public`), so login fails locally; verification was limited to a clean `tsc` + `vite build` on both workspaces.) (**Maintenance module: sequential service-order numbers, date rules, uploadable workshop invoices, and a rebuilt details page**: owner reported four problems on `/maintenance` and `/maintenance/:id` — a new schedule accepted start/end dates years in the past, creating one returned a bare `Internal server error`, service orders had no human-readable number (the UI printed `#` + the first 8 chars of a UUID), and the details page was a tab bar (Work report / Invoice / Vehicle) with an oversized lifecycle stepper. **Schema** (`MaintenanceRecord.ref_id String? @unique`, hits production via `db push` on deploy): service orders are now numbered `MNT-001`, `MNT-002`, … through the existing `generateRefId` helper, which already gap-fills — so deleting `MNT-005` frees that number for the next order, exactly as asked. Because `ref_id` is unique across soft-deleted rows too, `deleteMaintenanceRecord` **clears** the ref_id on delete (otherwise the freed number would collide forever); `trashController`'s `MaintenanceRecord` restore branch therefore mints a fresh number when restoring an order whose ref_id was released. `prisma/seed.ts` gained an idempotent `backfillMaintenanceRefIds()` that numbers pre-existing records oldest-first and no-ops on every start after the first. **The 500 was never diagnosable from the response** — every `catch` in `maintenanceController.ts` swallowed the error and returned a generic `SERVER_ERROR`, so all five handlers now `logger.error({ err }, …)` and the real cause is visible in container logs. Three concrete 500 sources were fixed in the same pass: (1) `updateMaintenanceRecord` guarded its date conversion with `if (data.end_date)`, so an **empty string** — which is exactly what the edit dialog sends when a date is cleared — skipped the guard and was handed to Prisma as a `''` string for a `DateTime` column; a shared `toDate()` now maps `''`/`null`/`Invalid Date` to `null` for the nullable columns and drops the key entirely for the non-nullable `start_date`/`service_date`. (2) A `vehicle_id` that parsed as a UUID but pointed at a deleted/non-existent vehicle hit a Prisma FK violation; create now looks the vehicle up first and returns a 400 saying so. (3) `odometer_reading`/`cost` are now `Number.isFinite`-checked before reaching a `Float` column. **Date rules**, enforced in both places per CLAUDE.md: a new `validateDateWindow()` rejects a back-dated start or end on **create only** (an existing record legitimately holds old dates — editing an unrelated field must not fail because of them) and rejects `end_date < start_date` / `next_service_due < start_date` on both create and update; the web forms mirror it with `min` attributes (`min={TODAY_ISO}` on a new order's start date, `min={start_date}` on the end date) plus pre-submit checks, and changing the start date snaps a now-invalid end date forward rather than leaving it silently wrong. **`MaintenanceDetailsPage.tsx` rebuilt**: the three-tab switcher is gone — everything is on one page as sections. The 28px-circle stepper card was replaced by a 4px lifecycle rail with three milestone dots and labels under it, and the four 2xl KPI cards shrank to compact metric tiles. The work report is now laid out as an actual workshop document: a letterhead band (workshop name, type, plate, order no., service date), then "Scope of work performed" as accent-ruled body text, "Technician remarks", and a "Service record" field grid. Vehicle and Workshop are clean sidebar cards (the vehicle's plate gets a dedicated block); Invoice is a full-width section splitting billing details from **uploaded workshop invoices** — the new part: the invoice is issued by the workshop, so it's now uploadable via the existing `POST /documents` (`entity_type: 'MaintenanceRecord'`, `doc_type: 'Invoice'`), listed with open/remove actions and a confirm dialog. Added a Print action for the report. The list page gained an `Order #` column (and the grid cards a ref line), search now matches on `ref_id`, and CSV export carries `Order_No` instead of the raw UUID. Verified: `tsc --noEmit` clean on api-server, `tsc -b` + `vite build` clean on web-dashboard. **Not exercised in a running browser** — Docker isn't running locally, so there is no local Postgres/API to log into; the create-path 500 in particular is fixed by reasoning plus the new logging, not by reproducing it.) Previously: **Fixed KPI counts changing when filtering on Vehicles, Drivers, and Invoices list pages**: clicking a KPI card or changing the status filter passed `status` to `getAll()`, which filtered the query response to only vehicles/drivers/invoices matching that status; because KPI values were computed from that filtered response, selecting any status caused all other KPI numbers to drop to 0 or collapse to the filtered count. Fixed by adding an un-filtered `kpiSummary` query (`['vehicles', 'kpi-summary']`, `['drivers', 'kpi-summary']`, `['invoices', 'kpi-summary']`) on `VehicleListPage.tsx`, `DriverListPage.tsx`, and `InvoiceListPage.tsx` — mirroring the pattern already used on `TripListPage.tsx` — so KPI instrument panel totals stay fixed and accurate across all cards while the list/grid/map filters independently. Verified: `tsc -b` and `vite build` clean on web-dashboard.) Previously: **Fleet Excel bulk import shipped (Milestone 3.5)**: the templates, the field-mapping guide and the *export* side had existed for weeks; the import half did not — no `POST /drivers/import`, no `POST /vehicles/import`, no upload UI. Rows are parsed **in the browser** with ExcelJS (already a dependency for export) and posted as JSON, the same contract `/trips/bulk-import` already uses, so no file-upload handling or spreadsheet library was added server-side and the operator gets a real preview before anything is written. **Backend**: `bulkImportDrivers`/`bulkImportVehicles` + `bulkImportDriversBody`/`bulkImportVehiclesBody` (1000-row cap). Upserts on `phone_primary` for drivers (the only unique column — `license_number` is *not* unique in the schema) and `plate_number` for vehicles, so re-uploading a corrected file fixes records instead of duplicating them. **Status is deliberately never written**: a re-import must not flip a driver or truck that is out on a job back to Available. Both run **two passes** — the drivers sheet references vehicles by plate and the vehicles sheet references drivers by phone/name, so whichever is imported first has dangling references; pass 1 creates the records, pass 2 resolves the links. An unresolvable link is a per-row **warning** (record imported, assignment skipped) rather than an error, because 200 drivers should not fail over one mistyped plate. Driver phone matching is digits-only with suffix comparison (`+966 50 123 4567` / `+966501234567` / `0501234567` all match) and strips a trailing parenthetical, since the shipped template's own sample writes `+966 50 123 4567 (Ahmed)` — the file would otherwise have failed against itself. Ambiguous name matches are refused with an explanatory warning rather than guessing. **Frontend**: `importUtils.ts` (header matching that ignores case/`*`/bracketed units, accepts several spellings per field, locates the header row *by content* since the templates put banner rows above the table, and flattens Date/formula/rich-text cells — dates emitted from local parts, not `toISOString()`, which shifts UTC and can move a date back a day) plus a shared `ExcelImportDialog` (drag-drop, preview of row count / missing required columns / ignored headers / first 3 rows, then a created-updated-failed result panel splitting hard failures from warnings). Wired into the Drivers and Vehicles list pages. **Two real bugs found and fixed while verifying against the actual template files**: (1) the vehicles template **could not be opened by ExcelJS at all** — its sheet name `🚛 Vehicles & Trailers Directory` is 32 UTF-16 units (an emoji counts as two) against Excel's 31-char cap, so readers truncate it and then throw `worksheet name already exists`; shortened in `scripts/generate_excel_templates.py` and regenerated. (2) The generator wrote to `frontend/public/templates`, but this is a Vite app where only `frontend/web-dashboard/public` is served — the templates had been generated to a folder served to nobody, so every in-app template download link 404'd; paths are now repo-root-relative instead of hardcoded `d:\Mercon\...`. Verified end-to-end by parsing both shipped templates with the real matching rules: all driver fields map, all vehicle fields except the optional `trailer_type` (which has no column) map, header row located at row 7 in both, and the 50 blank spacer rows are correctly skipped leaving only the 3 sample rows. Verified: `tsc --noEmit` clean on api-server, `tsc -b` + `vite build` clean on web-dashboard.) Previously: **Stop addresses are now visible and correctable on the web**: an operator could not see the address a driver had been given (`TripDetailsPage` rendered `location_name` only), and once a trip existed a wrong address was wrong forever — the only stop-level endpoint was `PATCH /trips/:id/stops/:stopId/delay`, and `EditTripPage` edited status/driver/vehicle and nothing else. New `PATCH /trips/:id/stops/:stopId` (`updateTripStop` + `updateTripStopBody`, every field optional since the usual case is fixing one address) updates the stop's label, full address, lane endpoint and pin. **Editable while the trip is in flight** — a wrong address is exactly what needs fixing mid-trip — and refused with 409 once the trip is Completed/Invoiced/Cancelled, because a finished trip is a record of what happened and its invoice is already priced off that lane. Timestamps are never touched: moving a pin does not un-arrive a driver, and the delay report reads arrival/departure, which stay as recorded. **Web**: `TripDetailsPage` now prints the stop's address under its name (falling back to the lane endpoint's address) and says *"No address — the driver only gets a map pin"* in amber when neither exists, rather than rendering a blank line; the lane endpoint's name is appended to the "Pickup Location"/"Drop-off Location" heading. New `StopAddressEditor` component drives a third section on `EditTripPage`, reusing `LocationCombobox` + the (already address-aware) `LocationPickerMap`; it saves per-stop on its own button rather than joining the manifest submit, since it hits a different endpoint and the common case is fixing one address without touching driver/vehicle/status. Closed trips render the stop read-only with an explicit reason. `TripStop` on the web service gained `location_address`/`locationId`/`location`. Verified: `tsc --noEmit` clean on api-server, `tsc -b` + `vite build` clean on web-dashboard.) Previously: **Locations management page** (`/locations`, new): the `/locations` CRUD API shipped without any UI, so places could only ever be born inline from a dropdown — nobody could see the full list, fix a misspelling, fill in a missing address, or retire an entry typed by accident, and those mistakes are invisible at the point of creation. `getLocations` now returns `_count` for `originRateCards`/`destinationRateCards`/`tripStops` (all filtered to non-deleted), which is what makes the page answer its two real questions: which places are actually used, and which are incomplete or abandoned. New `LocationListPage` + `LocationFormDialog` (`components/locations/`): four derived KPIs (total / on a rate card / missing address / unused), a banner when any place has no address (a stop defaulting to one hands the driver coordinates and nothing to read), search, and All/Unused/Missing-address filters. Renaming is the fix for a typo — the API already cascades the new name into every rate card quoting it, and the dialog warns before you do it; it is **not** a merge, so if both spellings already exist the rename is refused as a duplicate and the rate cards must be re-pointed by hand (noted as a gap, not built). Delete surfaces the API's own 409 verbatim ("used by N rate cards") rather than a generic failure, and warns separately when trip stops reference the place — those trips keep their own address and coordinates, only the picker entry goes. Sidebar entry added under FINANCE next to Rate Cards, since a location exists to be one end of a priced lane. Verified: `tsc --noEmit` clean on api-server, `tsc -b` + `vite build` clean on web-dashboard. **Not visually verified in a browser** — the local dev API refused connections, so login couldn't complete.) Previously: **Driver app now receives real from/to addresses; duplicate planned-start removed; Google Maps scoped**: owner reported that a dispatched trip reaching the driver's phone showed "no proper address", asked whether Google Maps is connected (**it is not — nothing in the codebase calls Google**), and flagged the same date being asked for twice on Create Trip. Root cause of the address gap was **not** the backend: `mobileTripController` has always returned full stop rows including `location_name`, but the mobile `TripStop` interface (`lib/trips.ts`) declared only `location_lat`/`location_lng`, so the app literally could not see the field and every driver screen rendered coordinates or nothing. **Schema** (additive, nullable): `TripStop.location_address` (full postal address of the exact yard — kept separate from `location_name`, which is the short label reports group by, because truncating an address into a label is lossy and irreversible) and `Location.address` (the verified address behind a lane endpoint, inherited by stops that default to that place). **Backend**: `mobileTripController` now has one shared `tripInclude` — its four endpoints each built their own before, some with `stops: true`, which returns stop columns but not the related `Location`, so *which endpoint the app happened to call* decided whether the driver saw a route or a pair of coordinates; stops now always carry `location {id,name,address}`. `createTripBody` accepts `location_address` (500-char cap, vs 120 for the name). **Driver app**: `TripStop` gained `location_name`/`location_address`/`location` plus `stopLabel()`/`stopAddress()` helpers (most-specific-first fallback: the stop's own address → its lane endpoint's address → null). Home screen's route timeline showed only "PICKUP"/"DELIVERY" and a time — it now leads with the place name and address; `LiveNavigationScreen`'s bottom card gained a "PICKING UP AT"/"DELIVERING TO" block (it was a blue line and a distance that never named the destination); `PickupVerificationScreen` gained a location card so the driver can confirm they're at the right yard before photographing cargo; `DeliveryVerificationScreen` shows "Delivered to" + address in the review step; the trips list gained a route line, since a customer name alone doesn't distinguish two runs for the same customer on the same day. **Web**: `LocationPickerMap` kept the full Nominatim `display_name` instead of discarding it after extracting the short label (this discard was the original reason no address ever reached a driver), and gained an editable address textarea so a hand-dropped pin can still be given one; Create Trip sends `location_address` per stop and pre-fills it from the chosen `Location`. **Duplicate date removed**: step 1's "Planned Trip Start (Optional)" block deleted entirely and `Trip.planned_start` now derives from the step 3 pickup arrival time — one moment, entered once, where previously the two were separate fields that could silently disagree. **Google Maps decision**: addresses only (Places API (New) + Geocoding) — map *display* stays on OpenStreetMap tiles because Google bills per map load and the app renders maps on tracking, trip details and several KPI cards. Not yet wired: needs an owner-provisioned key (account creation + billing is theirs to do); `docs/technical/GOOGLE_MAPS_SETUP.md` documents exactly which two APIs to enable, the referrer/API key restrictions, the budget alert, that Vite inlines the var at build time so it must also be a compose build arg, and the requirement that absence of the key falls back to Nominatim rather than breaking. **Known remaining risk, deliberately out of scope**: driver turn-by-turn still routes via `router.project-osrm.org`, a public demo server with no SLA. Verified: `tsc --noEmit` clean on api-server and mobile app, `tsc -b` clean on web-dashboard.) Previously: **Rate cards rebuilt around priced lanes, and wired to trip creation and invoicing**: owner reported the rate card module and its connection to Create Trip were wrong end to end. Root cause: `RateCard.route_origin`/`route_destination` were free text, and `CreateTripPage` matched them with a substring test against the *stop's* name — "Khamis Sorting Center" never contains "Riyadh", so matching fell through to "any active card for this customer" and finally to `rateCards[0]`, meaning the price shown was frequently not the price for that route. `tripLifecycle.completeTripAndInvoice()` then re-guessed the rate with a *different* rule (and a hardcoded `1000.0` fallback), so the invoice could quote a price the dispatcher was never shown. **Schema** (`schema.prisma`, hits production via `db push` on deploy): new **`Location`** model (`name`, `slug` unique = lowercased/whitespace-collapsed name so "Madina"/"Madinah"/"riyadh " can't become separate lanes, optional default `lat`/`lng`, soft-delete); `RateCard` gained `originLocationId`/`destinationLocationId` FKs (`route_origin`/`route_destination` kept as a denormalised copy of the names so old rows, exports and invoices still render) plus an index on `[customerId, originLocationId, destinationLocationId]`; `Trip` gained `rateCardId`; `TripStop` gained `locationId` (the lane endpoint "Riyadh", distinct from `location_name`/lat/lng which are the exact yard the driver navigates to). **Pricing rule, now single-sourced** in `services/rateLookup.ts` (`findRateForLane`, deliberately a service not a controller — importing a controller from `tripLifecycle` would pull `index.ts` and the express app in at module load): a card with `customerId: null` is the **standard** price for a lane and applies to every customer; a card with a customer **overrides** it for that customer only; order is customer's card → standard card → nothing. `createTrip` and `completeTripAndInvoice` both call it, and the flat `1000.0` invoice fallback was removed (an invented price is indistinguishable from an agreed one; an unpriced trip now invoices 0, which is visibly wrong and gets corrected). **Backend**: new `locationController`/`locationRoutes` (`/locations`, Admin+Operator) with `resolveLocation()` that creates-or-revives by slug and is shared with the rate card controller; renaming a location cascades into every rate card's denormalised names; deleting one is refused (409) while a live rate card still prices it. `rateCardController` rewritten — customer is now optional, lanes resolve from ids *or* names (so the trip wizard can name a new place inline), duplicate lanes per scope are refused with a message naming the clashing card, plus new `GET /rate-cards/lookup` and `POST /rate-cards/:id/assign` (copy a lane's price onto N customers; customers who already price that lane come back in `skipped`, never silently overwritten). `createTripBody` accepts `rate_card_id` and `stops[].location_id`. **Frontend**: new `locationService`, `LocationCombobox` (searchable, creates a place inline from the typed text), `RateCardFormDialog` (one add/edit form shared by the rate cards list, the details page and the customer page, so the standard-vs-customer choice is asked identically everywhere) and `AssignRateCardDialog`. Create/Edit Rate Card pages rebuilt on lane pickers with an explicit "applies to" choice (the create form previously *required* a customer, so the standard-lane case the schema already supported was unreachable from the UI). **Create Trip step 3** now picks an Origin and Destination `Location` above each map (picking one moves the map pin to that place's default coordinates and pre-fills the stop name, both still editable), looks the rate up server-side as customer+lane change, labels which tier matched ("Customer rate"/"Standard rate"), and when the lane is unpriced offers to save the typed price as the standard rate / for this customer only / not at all — creating the card before the trip so `rateCardId` is recorded, and dispatching the trip anyway if that save fails. Customer details page now lists the customer's own rates *and* the standard lanes they inherit (suppressing any standard lane they've overridden), with add/edit/override inline. **Invented content removed** (same rule as the maintenance page): `RateCardDetailsPage`'s "~480 km", "Est. Transit: 6h 15m", "24.0 Metric Tons", "Heavy Flatbed / Reefer / Lowbed", detention/overweight/night-premium fee schedule and "VERIFIED" badge — none of those fields exist in the schema and they printed identically on every card — replaced by a real "other rates on this lane" comparison; the customer page's rate block was rendering `rc.origin_city`/`rc.destination_city`/`rc.asset_type`, none of which exist on a rate card, so it had been showing "undefined ➔ undefined / Equipment: Flatbed"; the list page's fabricated KPIs (`'Riyadh → Jeddah'` default lane, `tripCount={42}`, `+4.2%`, `percentage: 90`, `|| 85`) and the create form's fake name/city presets ("SABIC Dammam Route", "Saudi Aramco Jubail Express"). Rate cards whose endpoints predate lanes are flagged "Not linked" in the list, on the details page and in the editor, because they can never match a trip until re-pointed. Verified: `tsc --noEmit` clean on api-server, `tsc -b` + `vite build` clean on web-dashboard.) Previously: **Maintenance details page (`/maintenance/:id`) rebuilt on real data only**: owner flagged the page for duplicated branding chrome and invented content. Removed from `frontend/web-dashboard/src/pages/maintenance/MaintenanceDetailsPage.tsx`: the "MERCON Logistics" chip and "Fleet Operations" badge in the header (the app shell already brands every page), the entire **"Itemized Checklist" tab** — its five rows ("Engine Oil & Filter Replacement", "Brake Pad & Rotor Inspection", …) were a hardcoded array rendered through `DataTable` as if they were recorded service items, when no such data exists in the `MaintenanceRecord` model — the "Compliance & Warranty → VALID/APPROVED" KPI (derived from nothing but `maintenance_type === 'Renewal'`), the "100% Tax Deductible" / "OPERATIONAL EXPENSE" / "Synced to Vehicle P&L" claims in the financial tab (no tax or ledger field exists anywhere in the schema), the "Commercial Heavy Commercial Workshop" descriptor printed under every workshop name, the fabricated `progressPercent` (100/50/15 by status) KPI, and the "Standard routine service and diagnostic inspection performed…" fallback that made a record with no `work_done` look like it had one — empty fields now render an em dash or an explicit empty state. Rebuilt UI on shadcn primitives: KPI row is now Cost / Odometer at service (with real km-driven-since delta vs `vehicle.current_odometer`) / Downtime (computed from `start_date`→`end_date`) / Next service due (the previously-unused `next_service_due` field), followed by a new Scheduled→In Progress→Completed lifecycle stepper driven by the record's actual status (with a distinct cancelled state) replacing the fake progress bar; tabs cut from four to three (Work report / Invoice / Vehicle), the raw `<textarea>` and hand-rolled skeleton markup swapped for the `Textarea` and `Skeleton` primitives, and status/type badge `switch` ladders collapsed into two lookup maps. The edit dialog gained the three fields it was silently dropping (`invoice_number`, `next_service_due`, `remarks`) and the invoice tab now surfaces `invoice_url` as a link when present. Verified: `tsc --noEmit -p tsconfig.app.json` clean on `frontend/web-dashboard`; not exercised in a running browser (no maintenance records exist locally — the seed deliberately creates no demo data). Earlier, 2026-08-08: **Driver now has a persistent "assigned vehicle"**: owner asked that picking a driver on the Create Trip wizard auto-fill that driver's usual vehicle instead of re-picking it every time. Added `Driver.assignedVehicleId` (optional, `@unique` so one vehicle maps to at most one driver at a time) as a named 1:1 relation to `Vehicle` in `schema.prisma` — a real schema change, hits production on next deploy. Backend: `createDriverBody`/`updateDriverBody` (`schemas/index.ts`) gained `assigned_vehicle_id` (uuid, nullable, optional); `driverController.ts`'s `getDrivers`/`getDriverById` now `include: { assignedVehicle: true }`, `createDriver` accepts it, and `updateDriver` (previously a blind `...req.body` spread) now explicitly maps the snake_case API field to the camelCase Prisma column since the two no longer match — both create/update paths surface a friendly "That vehicle is already assigned to another driver" message on the `P2002` unique-constraint hit. Frontend: `driverService.ts`'s `Driver`/`CreateDriverPayload` types gained `assignedVehicleId`/`assignedVehicle`/`assigned_vehicle_id`; `AddDriverPage.tsx` and `EditDriverPage.tsx` each gained an "Assigned vehicle" `Combobox` field (same pattern as the trip wizard的 vehicle picker); `CreateDriverModal.tsx` (the inline "Add driver" dialog used mid-trip-dispatch) gained the same field so a driver created on the fly can also get a default vehicle immediately. `CreateTripPage.tsx` gained a `useEffect` that sets `vehicleId` from the selected driver's `assignedVehicleId` when it's still in the `Available` vehicle list — pre-fills but doesn't lock the field, so the dispatcher can still override per trip. Verified: `tsc --noEmit` clean on both `backend/api-server` and `frontend/web-dashboard`; Prisma client TS types regenerated successfully, but the native query-engine binary swap (`prisma generate`) was blocked by a file lock from another session's running dev server — not restarted since it isn't this session's process to kill; not yet exercised in a running browser this session. Earlier the same day: **Trip Details styling pass to match `UI_GUIDELINES.md` (`frontend/web-dashboard/UI_GUIDELINES.md`)**: the owner said the page's cards/borders/shadows/curves "felt generic" — on inspection, all three previous Trip Details rewrites had used stock Tailwind `slate-*` grays (`text-slate-900/500/400`, `border-slate-200/100/300`) and `rounded-3xl`/`shadow-md` instead of this app's actual documented design system, which the rest of the app already follows: near-black/gray text tokens (`#111111` primary / `#6E6E80` secondary / `#9898A4` tertiary, not Tailwind slate), translucent-black borders (`border-black/[0.04]` to `/[0.08]`, never solid gray), `rounded-2xl` for cards (not `rounded-3xl`), `shadow-sm` only (never `shadow-md`/`lg`), and labels styled as `text-[10px] font-bold uppercase tracking-wider text-[#9898A4]`. Swept every card border, body/label text color, and label typography in `TripDetailsPage.tsx` to match (labels went from an inconsistent mix of `text-[11px] font-medium`/`text-[10px] font-semibold` to the one canonical label style used everywhere else in the app), downgraded the embedded `TripLiveMapCard`'s `shadow-md` to `shadow-sm` via its `className` override for this page, and swapped a couple of stray `bg-slate-100`/`bg-slate-50` fills for the app's `bg-black/[0.0x]` tint convention. Decorative multi-color icon chips (blue/violet/emerald for Customer/Driver/Vehicle) were left on Tailwind's palette since `Header.tsx`'s nav pills already establish that as an accepted pattern for categorical icon coloring, not covered by the gray/border rules. Verified: `tsc --noEmit -p tsconfig.app.json` clean; grepped for leftover `slate-` classes (one intentional exception remains — a neutral gray stop-marker circle for non-pickup/dropoff stop types, not a text/border violation). Still not exercised in a running browser this session. Earlier the same day: **Real bug found and fixed from a live screenshot review**: after the reference-screenshot rebuild deployed, the owner posted actual screenshots of `mercon.tech`'s Trip Details page for a real trip (Kozhikode → Kochi) — which surfaced that `TripLiveMapCard.tsx` always drew the hardcoded demo Riyadh↔Jeddah polyline and flew the camera to a demo truck's simulated position (`fleet.find(...) || fleet[0]` — the `|| fleet[0]` fallback ran for any trip that wasn't one of the ~4 canned demo trip refs), so the map showed an unrelated Saudi Arabia route/city label ("RIYADH") for a trip whose real pickup/dropoff pins were in India — a real bug pre-dating this rebuild, not introduced by it, just newly visible once someone looked at a non-demo trip. Fixed: `hasRealCoords` now gates behavior — when real pickup/dropoff coordinates are supplied and don't match a demo fleet entry, the polyline is a straight line between the real coordinates, no live-truck marker is fabricated (better to show nothing than a truck in the wrong country), and a new `FitBounds` helper (`map.fitBounds`) frames the real pickup/dropoff pins instead of flying to a fake position; marker popups also gained optional `pickupLabel`/`dropoffLabel` props so they read the trip's real `location_name` instead of the hardcoded "Riyadh Dry Port"/"Jeddah Islamic Port" strings. The canned demo trips (matched by `tripId`/`refId`) keep full simulated-telemetry behavior unchanged. Everything else from the screenshot review checked out correctly against the reference: status pill colors, the Stops table's per-row Pending/Upcoming logic, and the Activity panel's per-step icons (green check / blue-truck "departed" / orange active / gray pending) all matched real trip state exactly as designed. Verified: `tsc --noEmit -p tsconfig.app.json` clean; still not exercised in a running browser this session (same stuck preview-tab permission check), but this fix was validated by reading the owner's actual `mercon.tech` screenshots rather than guessing. Earlier the same day: **Trip Details page rebuilt a third time to match an exact reference screenshot**: the owner shared a screenshot of the actual app shell (same dark sidebar/branding) with a specific target content layout and said "exactly like this" — a persistent 2-column layout (not tabs replacing the whole page): left column stacks a trip-id/status/customer-driver-vehicle-ETA header card, a chrome-free live map, a horizontal pickup→drop-off route row, and a Stops/Documents/Audit-Trail underline-tab switcher (Stops now a real `Table` with numbered stop-type-colored circles, Scheduled/Actual Time columns, a status pill, and a click-to-expand row for delay-reason logging — replacing the previous vertical dashed timeline); right column is a persistent sidebar (not per-tab) with a Trip Progress card (big `%`, bar, route text, ETA, Distance/Elapsed/Stops/Avg-Speed stat rows, Call Driver/View Map buttons) and an Activity card (4-checkpoint live feed with per-step icons — green check, blue truck for "departed", orange pulse for the active step, gray outline for pending). `TripLiveMapCard.tsx` gained `showHeader`/`showTelemetryBar`/`className`/`mapHeightClassName` props (default true, so its only other nothing-else-uses-it call site here is unaffected in spirit) to render a plain chrome-free map per the reference, plus the Leaflet zoom control moved to bottom-right via `ZoomControl` (was default top-left) — the unused `ShieldCheck` import was also dropped in the same edit. All real-data-only and full-functionality constraints from the two passes before this hold: nothing in the reference's mock copy (fake trip IDs, "IN PROGRESS" wording, timestamps) was hardcoded, every field still reads from `Trip`/`TripStop`, and every action from the original page (assign/replace driver via `Popover`, delay logging, document upload, status/cancel modals, invoice link, audit trail) is still present, just laid out to match the screenshot. Verified: `tsc --noEmit -p tsconfig.app.json` clean on both changed files. **Still not verified in a running browser across all three passes of this rebuild** — the preview tab has been stuck behind an unresolved per-origin permission check for the entire session; this is the biggest open risk on this change (real spacing/proportions/wrapping have not been eyeballed against the reference, only inferred from source). Earlier the same day: **Trip Details page compressed into a single-screen operational cockpit**: after reviewing the premium rebuild below, the owner judged it had become a "feature inventory page" — 1,900–2,000px tall, admin-template smell (large stacked cards, equal visual weight for unrelated info, a KPI strip that doesn't drive decisions) — for a page dispatchers open dozens of times a day, where the real question is just "where's the trip, what's its status, who's on it, is anything blocked." Rebuilt `TripDetailsPage.tsx` around that: a condensed status strip (trip id/status, customer/driver/vehicle/ETA in one row, a mini "created → departed → in transit" activity line, progress % + bar, and Distance/Elapsed/Stops/Avg-speed as inline text stats — replacing the old separate hero card + KPI strip + 4-step horizontal timeline), the live map promoted to the dominant element beside a compact 4-checkpoint "Activity" panel (replacing the separate Route-details card), and an amber inline banner with inline driver/vehicle `Combobox` assignment that only appears when a Draft trip is actually blocked (replacing the always-visible bulky driver/vehicle assignment cards). Stops Log, Documents, and Invoice/Audit-Trail/Vehicle/Customer records moved behind shadcn `Tabs` (Overview/Stops/Documents/Audit) so only the active tab's content renders — no functionality was cut, everything from the previous rebuild (assign/replace driver via a `Popover`, delay-reason logging, document upload, status/cancel modals, invoice link, audit trail) is still reachable, just relocated out of the default scroll path. Verified: `tsc --noEmit -p tsconfig.app.json` clean; not yet checked in a running browser — the preview tab has been stuck behind an unresolved per-origin permission check across this session, so no live screenshot or click-through has happened for either this pass or the premium-design pass before it. Earlier the same day: **Trip Details page rebuilt to match a premium enterprise fleet-ops reference design**: owner supplied a detailed reference spec (breadcrumb + Edit/Print/More-Actions bar, a single hero `Card` with trip ID/status/customer/driver/vehicle/schedule grid plus an orange "Trip Progress" panel, a Route-timeline + live-map split, a 6-column KPI strip, and a 4-step Live Timeline) built on `frontend/web-dashboard/src/pages/trips/TripDetailsPage.tsx` with shadcn `Card`/`Badge`/`Progress`/`DropdownMenu`/`Tooltip`/`Separator`. The reference itself used fabricated sample data and silently dropped real functionality, so before building, the owner confirmed two constraints: (1) every value is sourced from the real `Trip`/`TripStop` model or computed from it, nothing invented — "Average Speed" is `planned_distance ÷ elapsed hours`, "Billing Amount" replaces the reference's fictional "Fuel Consumed" tile (`invoice.total_amount` or `trip.billing_amount`), and the `%` progress reuses the existing `STAGE_ORDER` stage-index calc; (2) all prior interactive functionality was kept and just reskinned, not dropped — driver/vehicle assign & replace, per-stop delay-reason logging, document upload, status-transition/cancel confirm modals, invoice link, and audit trail are still present (an Assignment/Invoice/Audit-Trail row + the detailed Trip Stops Log + Documents cards sit below the new hero/route/KPI/timeline sections). Also found and fixed a **latent bug** while rebuilding: `DashboardLayout` accepts `pageTitle`/`breadcrumb`/`actions` props but never renders them (a post-refactor pattern change, `git log` shows `a2b4325` moved page-level headers into each page's own JSX) — so the old page's header action buttons never actually appeared on screen. The new page renders its own breadcrumb + action row directly in `children`, matching the pattern `TripListPage.tsx` already uses. Verified: `tsc --noEmit -p tsconfig.app.json` clean; not yet checked in a running browser (owner interrupted the in-browser verification step to request commit+push instead — still needs a real click-through). Earlier the same day: **Trip Details page visual pass — real hierarchy, not just less gray**: owner feedback after the gray-fill cleanup was that the page was now "very plain," not "well designed" — confirmed no other page in the app is the bar to copy, so this raises the bar directly on `TripDetailsPage.tsx` along four axes the owner picked: stronger orange accent, typography hierarchy, visual progress indicators, tighter density. Route Overview header: ref ID bumped to `text-lg font-extrabold` behind an orange-tinted icon chip (`bg-[#E8450F]/10`), with a new single-bar **trip progress indicator** underneath (stage index over `STAGE_ORDER` = Draft→...→Invoiced, flat orange fill on a `bg-black/[0.06]` track, no gradient/glow/animation — deliberately not the removed multi-pill rail). The pickup/dropoff route line now fills green from 0→50→100% as `pickup.actual_arrival`/`dropoff.actual_arrival` land, instead of a static gray line; pickup/destination place names bumped to `text-base font-extrabold`. Distance stat chip gained a `border-l-2 border-l-[#E8450F]` accent edge and bigger number. Stops Log: dots are now 3-state (green = arrived, **orange = the next stop still pending**, gray outline = future) instead of binary green/gray, stop titles bumped to `text-[13px] font-extrabold`. Every right-column card header (Customer, Driver, Vehicle, Invoice, Audit Trail, Documents) gained the same `w-6 h-6 rounded-md bg-[#E8450F]/10` icon-chip treatment used elsewhere in the app (previously plain uncolored icons), and each card's primary value (customer/driver name, plate, invoice ref) bumped from `text-sm font-bold` to `text-base font-extrabold` for contrast against the `text-[#9898A4]` labels. Card gaps/padding were left alone (the shared `Card` component is used by every other page — tightening it here would leak into the whole app); density instead comes from the tighter `Separator` margins (`my-4`→`my-3.5`) already in place. Verified: `tsc -b` and `vite build` clean; not yet checked in a running browser. Earlier the same day: after the rebuild below, the owner reported the page still "looked the same" and flagged three concrete issues, all fixed. (1) **Real bug found and fixed**: some trips showed raw lat/lng instead of a place name for pickup/dropoff — traced to `frontend/mobile-app/mercon-app/src/screens/operator/CreateTripScreen.tsx`, where `isValid` required trip coordinates but never required `pickupName`/`dropoffName` (unlike the web wizard, which does), so an operator could submit a trip with no location name at all; `isValid` now also requires both names, and their `Input` labels are marked `*` required. The web `createTrip` endpoint and `bulkImportTrips` were confirmed as not the cause (bulk-import creates no stops at all — renders as "No pickup/dropoff stop on manifest", not coordinates). (2) The "Carrier" stat tile (always reading `carrier_name` / defaulting to "MERCON LOGISTICS") was removed from the Route Overview card — not needed per owner; Distance is now the only stat shown. (3) Gray/tinted fills that read as "AI slop" were replaced with the plain white-card + hairline-border look already established elsewhere (`border-black/[0.06]`/`border-black/[0.08]`, no fill): the Distance tile's `bg-gray-50`, the stops-timeline's `border-gray-100`/`bg-gray-300` dot, and the Documents row's `bg-gray-50/60` were all removed. The GPS-tracker "No tracker" `Badge`'s `bg-gray-50` was deliberately left as-is — semantic badge tints (vs. whole-surface fills) are the one place `UI_GUIDELINES.md` explicitly allows tinted backgrounds. Driver/vehicle assignment display was confirmed already present and correct (owner initially thought it was missing, then retracted). Verified: `tsc -b`/`vite build` clean on web dashboard, `tsc --noEmit` clean on mobile; not yet verified in a running app. Earlier the same day: **This is the single source of truth for "where is the project."*4: Last updated: **2026-08-08** (**Excel/PDF report exports rebuilt on real generator libraries**: the shared `frontend/web-dashboard/src/utils/exportUtils.ts` previously faked both formats — "Excel" was an HTML `<table>` saved with a `.xls` extension (`application/vnd.ms-excel` MIME + MSO XML comments), and "PDF" just opened a new window and called `window.print()` for the user to manually Save-as-PDF. Replaced with `exceljs` (real `.xlsx`, added as a new dependency) and `jspdf`+`jspdf-autotable` (real generated `.pdf`, downloaded directly, no print dialog). Two shared generators added: `exportExcelTable`/`exportPDFTable` (explicit title+headers+rows, used by pages that build a custom column set) and `exportExcel`/`exportPDF` (derives headers from a plain object array, mirroring `downloadCSV`'s existing header/value logic). Both classify each cell (currency/number/date/status-good/status-bad/text) from the header text and value shape to apply brand-colored (`#E8450F`) header bands, zebra striping, bold double-bordered totals rows, right-aligned `SAR #,##0.00` currency formatting, green/red status pill fills, frozen header + autofilter (Excel), and a branded footer with page numbers (PDF). Rewired every existing Excel/PDF export call site: `VehicleListPage.tsx`, `DriverListPage.tsx`, `CustomerDetailsPage.tsx` (customer trip ledger), `CustomReportPage.tsx` (custom trip ledger) for Excel; `TripListPage.tsx` (both the filtered-export dialog and the row-selection bulk action) for PDF — all now produce `.xlsx`/`.pdf` filenames instead of `.xls`. CSV exports were left as-is (already a real, correctly-formatted CSV). Verified: `tsc -b` clean on `web-dashboard`; a Node smoke test (stubbed DOM globals) drove `exportExcelTable` directly and the resulting `.xlsx` was unzipped and confirmed to be a valid OOXML package with real `xl/styles.xml` fills/fonts/borders, `autoFilter`, and merged title/subtitle cells — full in-browser click-through wasn't done this session (local API dev server's `nodemon`→`ts-node` spawn wasn't resolving `ts-node` from `PATH` in the preview harness, a pre-existing local-env issue unrelated to this change; `jsPDF`'s Node smoke test hit an unrelated Node/browser package-export-condition mismatch that doesn't affect the real Vite/browser bundle actually used at runtime). Earlier the same day: **`cargo_type` removed from Trips end to end, and Trip Details page decluttered**: per owner feedback that cargo type was never needed/used and the Trip Details page had "the worst UI in the app" with unnecessary noise, `Trip.cargo_type` was dropped entirely — `schema.prisma` (production data loss on next deploy, owner confirmed via "Implement"), `createTripBody`/`bulkImportTripsBody` zod schemas, `tripController.ts` (search filter, create, bulk import), `vehicleController.ts`'s vehicle-financials income-source rows, `seed-demo-local.ts`, every web dashboard display/type/CSV column (`TripDetailsPage`, `EditTripPage`, `TripTrackingPage`, `DashboardPage`, `CustomerDetailsPage`, `VehicleDetailsPage`, `TripListPage` incl. CSV import mapping/template, `exportUtils.ts`, `tripService.ts`/`vehicleService.ts` types, `CreateTripPage.tsx`'s hardcoded default), and the mobile app (`CreateTripScreen.tsx`'s required "Cargo" text input + validation — the only real cargo-entry UI anywhere in the app — plus every display/type site: driver Home/Trips/TripCompleted/Pickup+DeliveryVerification screens, operator TripList/TripDetails screens, `lib/trips.ts`/`lib/operator.ts`, `features/drivers` and `features/dashboard` types/services; `dashboardService.ts`'s Active-Vehicles-carousel `originLabel` — previously mis-sourced from `cargo_type` — now uses the trip的 `ref_id` instead). `TripCardSwiper.tsx`'s `cargoType` was deliberately left alone — it's unrelated hardcoded demo telemetry, never wired to real trips. Docs (`MOBILE_API_SPEC.md`, `final_database_schema.md`) updated to match; `api-contracts.md`'s `cargo_type` reference is a separate Rate Card field, untouched. Separately, **`TripDetailsPage.tsx` was rebuilt from scratch**, not edited in place: while this work was in progress, a separate commit on `main` (`e13cbab`, "revert(trips): remove Trip Details page to rebuild from scratch") deleted the old page entirely — along with the `/trips/:id` route, the `UserChip` component, and simplified `TripLiveMapCard`'s props — because the owner judged the old page's design bad, not just cluttered ("I deleted the previous page because it was shit... if you are creating back in the same design and same things, then that's a problem"). The rebuild keeps the same functional coverage (route overview, live map, stops log with delay logging, documents, customer/driver/vehicle cards with assign/replace, invoice, audit trail) but: the dark `#1C1C2E` "instrument panel" hero card is now a plain white `Card` matching the rest of the app (Customer/Dashboard pages already moved to this style); the cargo tile, the 6-step Draft→...→Completed status pill rail, and both "Edit Manifest" entry points (header button + "Add stops in Edit Manifest" link) are gone; and the **Financials/Settlement and Extra Driver Payment cards were cut entirely** per owner request (not just restyled) — their modals (`PostTripSettlementModal`, the cash-payout `ConfirmModal`), state, and mutations were removed from the page. `router.tsx`'s `/trips/:id` route and `UserChip.tsx` were restored (both needed by the rebuilt page); `tripService.ts` regained the `replaceDriver` call and the `Trip`/`TripStop` fields (`created_by`, `updated_by`, `updatedAt`, `vehicle.capacity_kg`, `vehicle.icces_device_id`, `stop.delay_logged_by`) that the deletion commit had trimmed along with the old page, since the rebuilt page still uses them for the audit trail, GPS-tracker badge, and delay "logged by" chip; the `TripLiveMapCard` call site was updated to that commit's simplified prop interface (just `tripId`/`refId`/pickup+dropoff lat-lng, no more `status`/`vehiclePlate`/`hasTracker`/`lastLat`/`lastLng`/`pickupName`/`dropoffName`). `CustomerDetailsPage.tsx` and `DashboardPage.tsx` had their own independent conflicting redesigns from the same period (both had already moved their trip tables onto the `DataTable` component) — those were kept as-is, just with their `Cargo Type` columns dropped. Pickup/dropoff address display needed no change: `TripStop.location_name` is captured via the Create Trip form's Nominatim address search and was already preferred over raw lat/lng, with coordinates only a fallback for stops never given a name (e.g. bulk-imported trips). Verified: both `tsc -b` and a full `vite build` pass on `frontend/web-dashboard`; not yet verified in-browser. Earlier the same day: **Trips KPI cards redesigned as clean stat tiles**: `KpiCard.tsx` rewritten — the sharp `rounded-none` "instrument panel" card (corner ticks, hairline accent rule, mono fonts, and a different gadget crammed into each card: semi-circle arc gauge, animated fake-74% pulse bar, donut ring, mini stage boxes) was replaced with one quiet tile that follows `UI_GUIDELINES.md`: `rounded-2xl`, `border-black/[0.06]` + `shadow-sm`, 10px uppercase muted label, 28px bold sans value, tinted rounded-xl icon chip, optional trend pill, and a single restrained footer visual per data shape (slim segmented distribution bar + dot legend, thin single-hue completion meter, live-pulse text row, inline stage counts, or a sparkline — the always-on fake `defaultChartData` sparkline was removed; sparkline now renders only when real `chartData` is passed). All existing props kept so the ~15 other pages using `KpiCard` compile unchanged; the `SemiCircleArcGauge` component was deleted (the `semiCircleGauge` prop now renders as the segmented bar). On the Trips page itself: meaningless decorative trend chips ("↑ All Operations", "→ Scheduled") removed, and TOTAL TRIPS' hardcoded fake sparkline data replaced with the real Completed/In-Transit/Queue status breakdown. `tsc --noEmit` clean; not verified in-browser this session. Earlier that day: **Drivers list actions reworked**: replaced the `Actions` dropdown trigger on the Drivers list table with inline action icon buttons (View, Quick Status, Docs, Edit, Delete) matching the style and layout pattern used in the Trips list table. Earlier that day: **Trips page: Export reworked as a dropdown menu**: replaced the modal-based export flow — which showed generic "All Drivers"/"All Vehicles" `Select`s instead of letting the owner actually pick one, and read as visually inconsistent with the rest of the app per owner feedback — with a `DropdownMenu` matching the pattern already used on Drivers/Invoices/Rate Cards list pages: a CSV/PDF format toggle at the top (emerald `FileSpreadsheet` / rose `FileText` icons), a quick "All Trips" item, "By Status" items (Completed/Delivered, In Transit, Not Completed), and "A Specific Driver"/"A Specific Vehicle" submenus that list actual driver names and plate numbers rather than a scroll-to-find select. Only date-range picking stayed in a dialog (now much smaller — just status + from/to date + the same format toggle), since a date range doesn't fit a menu item. `handleExport` was generalized into a parameterized `runExport(format, opts)` so both the dropdown items and the date-range dialog share one code path. Typechecks clean; not verified in-browser this session (preview pane wouldn't composite frames). Earlier the same day: **Trips page: CSV import added**: new "Import" button opens a dialog that bulk-creates `Draft`/`Dispatched` trips from an uploaded CSV — customer matched by name, driver/vehicle optionally matched by name/plate (each row independent; a bad row is reported with a reason, not fatal to the batch). Backend: `POST /trips/bulk-import` (`bulkImportTrips` in `tripController.ts`, `bulkImportTripsBody` zod schema, capped at 500 rows/request) reuses the same claim-driver/claim-vehicle transaction logic as `createTrip` but skips the stops requirement — imported trips have no route stops until added later through the normal trip edit UI, since a CSV can't reasonably carry map coordinates. Frontend: `parseCSVFile` added to `exportUtils.ts` (quote-aware CSV parser, header normalization), a downloadable template button, and a results view (imported/failed counts + per-row error list). Also **Export CSV/PDF buttons recolored** per owner request — green (emerald) for CSV, red (rose) for PDF, applied consistently to the Export dialog buttons and the row-selection bulk-action buttons (`DataTable`'s `BulkAction` gained an optional `className` passthrough to support it). Typechecks clean on both workspaces; not verified in-browser (dashboard requires auth, preview pane wasn't rendering this session). Earlier the same day: **Trips page: interactive KPIs + unified export + "New Trip" label**: the "New Trip Draft" button on `/trips` is now just "New Trip" — trip creation was never a separate draft-then-publish flow, `Draft` is simply the `TripStatus` a trip starts in when it's created without both a driver and vehicle assigned (scheduling via `planned_start`/`planned_end`/stop `planned_arrival` was already independent of that status and untouched here). The four KPI cards (`KpiCard.tsx` gained `isActive`/`onStageClick`/`onHealthClick`) are now clickable to filter the trip list by status/route-health/pipeline-stage, with an active ring style; KPI counts were also fixed to compute from the full unfiltered trip set (`allTripsRes`, a new unpaginated query) instead of just the current page. The separate Export CSV/PDF buttons were replaced with one Export dialog (status group, driver, vehicle, date range), and `GET /trips` (`tripController.ts`) plus `tripService.ts` gained a `vehicle_id` filter to support it. Not verified in-browser (dashboard requires auth; another session had the dev server on this port). Earlier the same day: **site-wide `--font-mono` set to JetBrains Mono**: `frontend/web-dashboard` added `@fontsource-variable/jetbrains-mono` (self-hosted, matching the existing `@fontsource-variable/geist`/`inter` pattern) and declared `--font-mono` in `index.css`'s `@theme inline` block, which previously had no override — Tailwind's `font-mono` utility (used across 41 files for plate numbers, ref IDs, timestamps, currency) was falling back to the default OS-monospace stack. `--font-sans` (Geist-based) and the separate "Geist Mono Numbers" digit-only `@font-face` hack were left untouched — out of scope for this change. Verified: `font-mono` utility now resolves to `'JetBrains Mono Variable'` via computed styles, the woff2 loads with a 200 from `node_modules` locally (self-hosted, no CDN), and both `tsc --noEmit` and a full `vite build` pass. Not checked past the login screen in-browser (dashboard requires auth). Earlier the same day: **Active Freight Trips Carousel hover effects toned down** — the lift/glow/gradient-overlay hover animations added in the carousel rebuild below (translate-y lift, colored shadow glow, scale-in accent rail, gradient map overlay, rotating gauge icon, sliding CTA arrow, animated ping dot, gradient corridor/progress fills) were replaced with a plain border-color + `shadow-sm` transition per owner feedback ("that's AI slop"), and the card background moved from `#FAFAFA` to white with a visible border instead of a faint ring, matching the app's existing restrained style. Earlier the same day: **Active Freight Trips Carousel rebuilt on shadcn**: `components/trips/TripCardSwiper.tsx` (dashboard) moved off its hand-rolled `overflow-x-auto` + scroll-listener carousel onto the shadcn **`Carousel`** primitive (`components/ui/carousel.tsx`, added — embla-carousel-react, already a dependency), gaining drag, arrow-key nav, snap-accurate index tracking and clickable dot indicators wired through a new `useCarousel()`-consuming `CarouselToolbar` in the card header (`CardAction` slot). Three more shadcn primitives added to support it: **`avatar.tsx`** and **`hover-card.tsx`** (both on `@base-ui/react`, matching the repo's `base-nova` style — hover-card wraps Base UI's `PreviewCard`) and **`skeleton.tsx`**. Each trip card was redesigned: driver tile is now an `Avatar` + `HoverCard` (phone/plate/cargo on hover), vehicle tile a `Tooltip`, the route corridor uses `Separator` with pin/flag icons, the micro-map gained a gradient hover overlay showing km-remaining + ETA, and progress now shows distance done/left. Hover/focus/active animations are shadcn-idiomatic throughout (lift + ring + shadow on the card, a scale-in accent rail, icon transforms, animated dot width) and cards are keyboard-focusable (`Enter`/`Space` to open). Also fixed a latent bug: the progress bar的 `[&>div]` selector was coloring the **track** rather than the indicator, so every bar rendered visually full — it now targets `[data-slot=progress-indicator]` with a gradient fill. Verified by `tsc --noEmit` and a full `vite build`; not verified in-browser (the dashboard is behind login). Earlier: **Trip creation: "assign driver/vehicle later" added**: `POST /trips` no longer requires `driver_id`/`vehicle_id` — a trip created without one or both now lands in `Draft` status instead of `Dispatched` (driver/vehicle claiming and the assignment notification are skipped for whichever is missing). `/trips/:id/dispatch` (`dispatchTrip`) reworked to accept either field independently rather than requiring both, so a Draft trip can have its driver and vehicle assigned in separate calls; it only flips the trip to `Dispatched` once both end up set. `updateTripStatus` gained a guard rejecting a direct Draft→Dispatched status change when the trip still has no driver or vehicle (that path must go through `dispatchTrip`, which does the atomic Available→OnTrip claim) — web's `TripDetailsPage.tsx` mirrors this by hiding the "Mark Dispatched" quick action until both are assigned. Web changes: `CreateTripPage.tsx` (`/trips/new`) step 2 gained "Assign driver later"/"Assign vehicle later" checkboxes that disable the respective picker and relax the step/submit validation; `TripDetailsPage.tsx`'s driver/vehicle cards now show an inline assign-now picker (calling the reworked dispatch endpoint) when a Draft trip is missing one; `Combobox` (`components/ui/combobox.tsx`) gained a `disabled` prop to support the checkbox-disables-picker interaction. Not yet run against a live server for E2E verification — a concurrent session's in-progress `hazmat_flag` removal (`schema.prisma`) had the local API mid-edit and returning 500s on login at the time; both `backend/api-server` and `frontend/web-dashboard` typecheck clean (`tsc --noEmit` / `tsc -b`). **Hazmat removed entirely from the project**: `hazmat_flag` dropped from `Trip` in `schema.prisma` (production data loss on next deploy — owner confirmed), the zod schema, `tripController.createTrip`, both web (`tripService.ts`, `CreateTripPage.tsx`, `TripListPage.tsx` badge + KPI breakdown, `exportUtils.ts` CSV column) and mobile (`operator.ts` types, `TripDetailsScreen.tsx` cargo row) call sites, and the unrelated static "Hazmat Clearance" placeholder badge on `RateCardDetailsPage.tsx`. The IN-TRANSIT KPI's on-schedule/delayed breakdown had been (mis)computed from `hazmat_flag` as a stand-in for "delayed" — there is no real delay signal at the trip level (only per-stop `delay_reason`), so it now reads on-schedule = active-in-transit minus stopped, delayed = 0, until a real delayed-trip metric is built. Earlier the same day: web dashboard **removed the small uppercase "Module" badges next to page titles** — e.g. "Tariff Module" on Rate Cards, "Operations Module" on Trips, "Fleet Module" on Drivers/Vehicles/Maintenance, "Finance Module" on Invoices/Payment Status, "Commercial Module" on Customers, "Platform Module" on User Management, "System Administration" on Settings, "Dashboard Overview" on the dashboard greeting, "Fleet Roster" on Vehicles, "Operations Alert System" on Notifications — across 10 pages, per owner request; the `<h1>` title and the "Scope: ..." subtitle line beneath it were left untouched. Earlier the same day: web dashboard **fixed User Management's missing header/action buttons**: `DashboardLayout` never actually renders its `pageTitle`/`pageSub`/`actions` props — they're accepted but unused in the component body, so any page still passing them (only `UserManagementPage` did) silently got no page header and no action buttons at all, regardless of role. Reworked the page to render its own header row + Admin-only "Add Driver"/"Invite User" buttons inline, matching the pattern every other list page already uses (`DriverListPage`, `CustomerListPage`, etc: icon + title + badge + subtitle row, followed by `Button`s, all inside `children` rather than layout props). Also tightened permissions per owner request: Operators now see Admin/Operator rows only (no Drivers) and are view-only (no create/edit/delete/Add Driver); only Admin gets those actions and the Drivers rows. Backend `userRoutes.ts` split so `GET /users` allows Admin+Operator but `POST/PUT/DELETE /users` require Admin. Verified both roles by injecting a synthetic session client-side (no real credentials used) and reading the rendered page. Earlier the same day: added **`.github/workflows/ci.yml`**: a new hosted-runner (`ubuntu-latest`) CI workflow that runs on every PR into `main` and every push to a non-`main` branch — `npm ci`, `npm run lint -w @mercon/web-dashboard` (oxlint), then `npm run build` (build:types → api-server `tsc` → web-dashboard `tsc -b && vite build`), so a broken build/typecheck is caught before merge instead of failing on the self-hosted deploy runner. This is deliberately a separate workflow from `ci-cd.yml`, not a split of it: `ci-cd.yml` stays a single job because its steps are an ordered, stateful deploy sequence (containers must be healthy before nginx reloads) with no parallelism to gain on a single self-hosted runner — splitting *that* would only add fragile cross-workflow ordering. `ci.yml` is unrelated in trigger (PR/branch, not `push: main`) and runner (hosted, no secrets/prod access needed) so it's a genuinely separate concern, not a job split. Earlier the same day: backend **fixed a brief production outage**: the `ilan` seed commit below had hardcoded `phone: '+966500000003'`, which collided with an existing user's phone (unique constraint) already in the hosted DB; the container's startup chain is `prisma db push && seed.ts && node dist/index.js`, so the thrown seed error meant `node dist/index.js` never ran and `mercon.tech/api/*` 502'd entirely until the fix (drop the hardcoded phone — it's optional on `User`) deployed. Also added a permanent diagnostic step to `ci-cd.yml` that prints `mercon-api` container logs after every deploy so a startup crash is visible in the Actions run instead of a bare 502. Verified: `/api/health` 200 and `POST /api/auth/login` for `ilan`/`ilan1234` returns a valid token with role Admin. Earlier the same day: backend **`prisma/seed.ts` now also upserts a third canonical account, `ilan` (role Admin)**, added at the owner's explicit request alongside the existing `admin`/`operator` upserts — idempotent, own bcrypt hash, existing accounts still never touched. Earlier the same day: web dashboard **`/trips/new` (Create Trip wizard) restyled to match `/drivers/new` and `/vehicles/new`**: the 3-step wizard structure is unchanged (Customer → Driver/Vehicle assignment → Route/geofencing, the last step embeds `LocationPickerMap` so it wasn't collapsed into the single-viewport 2-column layout used on the other two pages), but every hardcoded `slate-*`/`indigo-*`/`rose-*` class was swapped for theme tokens, the step-manifest strip's boxed indigo icon circles were removed in favor of bare icons (matching the orange bare-icon convention already applied to page headers elsewhere), and the error banner now uses the shared `Alert` primitive instead of a hand-rolled rose div. Earlier the same day: web dashboard **User Management (`/settings/users`) opened up to Operator** (previously Admin-only) and turned into a combined "all platform users" view: it now also lists Drivers by querying `/drivers` alongside `/users`, shown read-only (a "View" action links to `/drivers/:id`) since Driver accounts have no login and are still only created/edited through the Drivers module; an "Add Driver" button links to `/drivers/new`. Backend `userRoutes.ts` changed from `authorizeRoles('Admin')` to `authorizeRoles('Admin', 'Operator')` on all `/users` routes; `createUserBody`/`updateUserBody` still restrict `role` to `Admin`/`Operator` only, so Driver-role web accounts remain non-creatable via this form, per the existing Phase-1 rule. `CLAUDE.md`'s "Who uses which app" table updated to match. Earlier the same day: web dashboard **`/vehicles/new` (Add Vehicle) redesigned to match `/drivers/new`**: same 2-column shadcn layout — form card (asset identifier / payload & telematics / optional trailer, separated by `Separator`s) beside a sticky "Registration summary" card with a live 4-item requirements checklist (plate, tractor capacity, trailer config, telematics); the old horizontal preview-strip + narrow single-column card + hardcoded slate/indigo classes were replaced with theme tokens and the shared `Alert` primitive for errors. Earlier the same day: **`/drivers/new` (Add Driver) redesigned** the same way: the full-width preview strip + narrow stacked form became a 2-column shadcn layout — form card (personal profile / commercial licence, separated by `Separator`) beside a sticky "Onboarding summary" card with a live 4-item requirements checklist, so the whole page fits one viewport with no scrolling; errors now render in the new shadcn `Alert` primitive (`components/ui/alert.tsx`, added), the page is a real `<form>` with a submit button, hardcoded slate/indigo classes gave way to theme tokens, a triple Ctrl+Enter submit binding (window listener + two `Btn` shortcuts, all firing the mutation) was reduced to one, and inputs/labels/headings were later sized up a notch (h-11 inputs, text-sm labels) for readability — both pages share that larger sizing. Also that day: **hosted demo-data workflow added**: `.github/workflows/demo-data.yml` loads the ~3,100-trip Delay Report demo dataset into the hosted database and removes it again — `workflow_dispatch` only, two actions (`seed`/`purge`), a typed `SEED`/`PURGE` confirmation checked before anything runs, and a `demo-data` GitHub Environment for a required reviewer. Both scripts run via `docker exec` inside the deployed `mercon-api` container (verified the image ships `scripts/` and `ts-node` — it already runs `npx ts-node prisma/seed.ts` at startup), and `DEMO_RESET` is never passed, so the seeder's destructive path is unreachable from CI. Row counts print before and after, tagged vs total. `ci-cd.yml`, `prisma/seed.ts` and `docker-compose.yml` untouched. Not yet run — the self-hosted runner is the only thing that can reach the production database. Previous session, 2026-08-05: mobile **operator Customers screen rebuilt** on `features/customers` at `/operator/customers` (More → Customers): KPI rail, server-side search/filter, sort, infinite scroll, per-customer trips/revenue/rate-card aggregated from `/trips`+`/invoices`+`/rate-cards`, and a role-gated overflow menu with optimistic suspend/delete; generic `SortDropdown` + `useDebouncedValue` extracted to `shared/`. Also merged in, same day: the **Delay Reporting module** built end to end — a `/reports/delays` page with three views behind one filter bar: the delay log (one row per late arrival, click-through to the trip), an on-time % grid switchable by route/driver/company/truck, and an analysis tab with KPIs, an hours-lost trend and four leaderboards, all at any period from day to year with a delta against the preceding window. Backing it: `TripStop.actual_departure`, `location_name`, and `delay_reason`/`delay_note`/`delay_logged_by`/`delay_logged_at` + a `DelayReason` enum; three `/reports/delays*` endpoints; and an operator alert that fires the moment a stop is reached 30+ min late. **Two real data bugs fixed underneath it**: the mobile/geofence status path never stamped `actual_arrival` at all (and dropoff arrival was backfilled at completion, dating every delivery to when its paperwork finished), so the database could not tell "arrived 2h late" from "arrived on time and waited 2h to unload"; and the mobile operator's Create Trip screen sent no planned times, so every trip booked from a phone was invisible to delay reporting. Reason capture is operator-only by design — drivers already report causes in the WhatsApp group. Verified end to end over real HTTP against a live Postgres, plus a browser walkthrough of the report page on a seeded dataset. Earlier the same day: mobile **operator Home + Drivers list screens rebuilt** on a new `features/dashboard` and `features/drivers` architecture — NativeWind + TanStack Query added to the mobile app for the first time (babel/metro/tailwind config, `QueryClientProvider` in root layout); Home is now AppHeader/context chips/search/scanner, two `DashboardMetricCard`s (Active Trips, Delayed Deliveries), an `ActiveVehiclesSection` carousel (driver+route+status per active trip), and a `DocumentExpirySection` (replaces the old Fleet Utilization block) with real expiring-document counts + a scoped All/Vehicles/Drivers/Company list; Drivers list gained two dark KPI tiles, search/status-filter/sort, and backend-paginated infinite scroll. Then: mobile **operator Driver Details screen** — new `features/drivers` screen at `/operator/driver-details?id=` with profile/statistics, assigned vehicle, contact + licence info, documents, current assignment, performance summary and call/message actions, built on a layered api→services→hooks stack with React Query caching, pull-to-refresh and optimistic status updates; the driver list now taps through to it. Earlier the same day: web dashboard **mobile responsiveness pass**: the fixed 220px sidebar became an off canvas drawer below `lg` driven by a header hamburger, the overflowing 8-item route pill bar is now desktop-only with Quick Create promoted to a header `+`, `DataTable`/`dialog.tsx`/`PageTitle` made phone-friendly, and page containers + stray fixed grids given mobile-first breakpoints. Also fixed a **pre-existing syntax error on `main`** — `handleUpdateStatus` in `DriverListPage.tsx` was missing its closing `};`, which made `tsc -b` fail and would have broken the Docker build/deploy. Previous session, 2026-08-04: fleet data onboarding: Excel export on the Drivers/Vehicles list pages now includes the driver's assigned vehicle plate and the vehicle's assigned driver — backend `getDrivers`/`getVehicles` now `include` the active trip + its vehicle/driver; styled `.xlsx` bulk-entry templates + `docs/MERCON_Fleet_Import_Guide.md` reworked to match those columns. Note: the templates are for **manual collection today — there is no backend import endpoint yet**. Previous session, 2026-08-02: mobile operator UI-kit consistency pass across all 8 operator screens + fixed a duplicate-bottom-nav bug; added real trip lifecycle actions (pickup arrive/verify, delivery verify, replace driver, cancel) to `TripDetailsScreen`; added Driver/Vehicle edit screens; added invoice "Mark as Paid"; added full Customers screen (list/search/create/edit); added an `Operator` "More" hub screen — fixed a real navigation gap where Vehicles/Invoices/VehicleRenewals were only reachable via deep link, not from the UI) · Owner: Hysam (solo dev + AI) · Deadline: ~1 month from July 2026

> ⚠️ **Keep this file honest.** It is written from reading the actual code, not the
> docs (the `docs/` folder describes the *planned* product and overstates progress).
> See the **[Update protocol](#update-protocol)** at the bottom — this file must be
> updated in the same change as any code/schema/endpoint/screen change.

---

## 1. TL;DR — where we are

| Area | Status | Done |
|---|---|---|
| **Backend API** | ✅ Working, deployed at mercon.tech | ~90% |
| **Multi-client productization** (Settings/superadmin/branding/modules, web de-branding, mobile per-client config, deploy parameterization) | 🔄 Foundation built and verified locally, module toggles now enforced backend-side too (not just hidden in the UI); deploy parameterization now proven live with a second real stack (`dev.mercon.tech`, isolated via `docker compose -p mercon-dev` + `CLIENT_NAME`, see §5) — `ci-cd.yml` tag-based rework, granting/revoking superadmin, and MTL's actual domain features still pending | ~68% |
| **Web dashboard** (Admin/Operator) | ✅ Done, all pages on real data | ~95% |
| **Mobile app** (Driver + Operator) | 🔄 Driver side ~done (nav + full trip flow + all core screens); operator side wired with real trip lifecycle actions (dispatch→pickup→delivery→complete, replace driver, cancel), Driver/Vehicle edit, Invoice mark-paid, and a new Customers screen (list/create/edit); a proper "More" hub now makes Vehicles/Invoices/Customers/Renewals reachable in-app (previously only via deep link); only secondary Replacement/Splash screens left static; live GPS pending | ~76% |
| **Live GPS tracking** (driver → web) | 🔄 Foreground streaming wired (socket emit); background + device verification pending | ~60% |
| **Delay reporting** (log + on-time grid + analysis) | ✅ Built, verified end to end | ~90% |
| **Testing / builds / handover** | ❌ Not started (no real-phone run yet) | 0% |

**One-line status:** Backend and web are basically finished. Trips assigned a driver + vehicle at
creation are auto-dispatched immediately (driver notified); either can now be left for "assign later",
landing the trip in `Draft` until both are filled in via the dispatch endpoint. On **mobile**, the whole **driver
side is real** — navigation, a straight-through trip flow (photo → live map with GPS auto-arrival →
POD → done), notifications, profile, documents, vehicle, emergency, and foreground **live GPS**
streaming. The **operator side is now navigable and wired**: Admin logs into the same operator nav
as Operator, `/operator/{trips,trip-details,create-trip,drivers,vehicles,invoices,vehicle-renewals}`
routes exist, the bottom nav + FAB actually go somewhere, trip cards tap through to a real
trip-details screen (real timeline/cargo/driver/vehicle, call-driver, "coming soon" for live
tracking/edit), Create Trip posts to `POST /trips` against real customers/available drivers/vehicles,
and Vehicle Renewals (reachable from the Home "docs expiring" alert) shows real expiring vehicle
documents. For **owner onboarding**, styled drivers/vehicles Excel templates + a field-mapping guide
exist and the web list pages now export the same columns (incl. assigned vehicle/driver) — but
**there is no import endpoint yet**, so a filled workbook still has to be entered by hand.
Remaining: bulk-import endpoint + UI, GPS background hardening + device verification, then
real-phone testing + release builds.

---

## 2. ✅ Completed

### Backend (`backend/api-server`)
| Piece | State |
|---|---|
| 19 controllers (auth, users, drivers, vehicles, customers, trips, invoices, rate cards, locations, documents, maintenance, notifications, reports, tracking, uploads + 2 mobile) | ✅ real DB logic |
| `GET /api/geocoding/resolve-maps-link` (`geocodingController.ts`, JWT-required): expands a Google Maps short link by following its redirects server-side, because a browser cannot read a cross-origin redirect target. The starting host is allowlisted to `maps.app.goo.gl`/`goo.gl`/`g.co` and redirects are capped, so this is a Google-link expander and not a general-purpose URL fetcher | ✅ (2026-08-20) |
| Prisma schema (12 models, 10 enums), PostgreSQL, idempotent seed (admin + operator accounts only — see §6, fake demo data seeding was added and removed same day) | ✅ (2026-07-29) |
| **Defensive Database Schema Cleanup & `driver_payout` Migration** (2026-09-13): Renamed `Trip.trip_charges` column to `driver_payout` mapped to column `"driver_payout"` via SQL DDL (`ALTER TABLE "Trip" RENAME COLUMN "trip_charges" TO "driver_payout";`). Removed obsolete flat fee columns `waiting_labor_charges` and `additional_stop_charges` (replaced by itemized `TripCharge[]` relation). Enforced Prisma `@relation` definitions across 7 models (`Trip.originalVehicle`/`originalDriver`, `TripStop.delay_logged_by_user`/`executorDriver`, `Document.executorDriver`, `TripAssignmentEvent.changedByUser`, `QuotationHistory.changed_by_user`, `MaintenanceRecord.workshop`). Retained `QuotationHistory.changed_by_name` as historical audit snapshot. **Troubleshooting & Diagnostic Checklist**: If any issue arises, run (1) `scripts/pre-migration-audit.ts` to verify orphan counts, (2) `scripts/verify-db-integrity.ts` to verify column existence & historical driver payout totals, and (3) `scripts/verify-runtime-and-relations.ts` to verify relation queries and financial math calculations. Response DTOs retain fallback `driver_charge`/`trip_charges` getters for backward compatibility. | ✅ (2026-09-13) |
| **Lane-based pricing**: `Location` model + `RateCard` origin/destination FKs; one matching rule in `services/rateLookup.ts` (customer's rate for the lane → none) shared by `GET /rate-cards/lookup`, `createTrip` and invoicing; `Trip.rateCardId` records what was quoted so the invoice can't disagree with it | ✅ (2026-08-09) |
| **Tonnage tier / trip-type now filters rate lookup, not just customer+lane**: `findRateForLane` accepts `vehicleType`/`rateCategory`; `Trip.vehicle_type`/`Trip.rate_category` record the dispatcher's actual selection; validated app-side (Zod) against the real known-value list (`VEHICLE_TYPES`/`RATE_CATEGORIES` in `@mercon/shared-types`), not a hard Postgres enum | ✅ (2026-08-11) |
| **"Standard" (all-customers) rate cards removed**: `RateCard.customerId` is now required — every rate is one customer's negotiated price, since every quote in the carriers' actual contracts is customer-specific and the owner confirmed no standard rates exist in production (deleted before this change). Removed the standard/customer scope choice from `RateCardFormDialog`/`CreateRateCardPage`/`EditRateCardPage`, the "Applies to" scope filter + KPI split on the Rate Cards list, `include_standard`/`scope` query params on `GET /rate-cards`, the "inherited standard lanes" section on the customer details page (and its dead "Override" flow), and the standard-fallback branch in `services/rateLookup.ts`. Deleted `TripStepStopsPricing.tsx`, an unused/orphaned duplicate of `TripStepRatesBilling.tsx` that still described the old concept | ✅ (2026-08-11) |
| **Itemized customer surcharges + driver payout per lane**: new `SurchargeRule` model (a customer's standing fee schedule — free-text `charge_type`/`unit`, optional `vehicle_type`, optional `rateCardId` scoping it to one lane vs. every lane for that customer) and `TripCharge` model (itemized charges actually applied to a trip, `quantity × rate` auto-calculated but overridable, denormalized at time of application so editing the rule later never rewrites a past trip's amount). Replaces the old `Trip.waiting_labor_charges`/`additional_stop_charges` — two hardcoded flat fields that couldn't hold the 6+ distinct fee types real customer quotations actually carry (additional stop, waiting/labor, labor-per-person, offloading-per-vehicle, same-day-delivery, trolley). New `RateCard.default_trip_charge` auto-fills `Trip.trip_charges` (the driver payout, not billed to the customer) at settlement — from the lane's agreed rate for MERCON's own driver, or from `Trip.third_party_cost` when the trip is outsourced — fixing a bug where trip creation had been seeding the payout field with the *customer's billing price* instead. New `POST/GET/PUT/DELETE /surcharge-rules` + `GET /surcharge-rules/charge-types` (distinct-values lookup powering the frontend's type-ahead-with-save combobox). Web dashboard: Rate Cards page gained a "Lane Prices" / "Surcharge Fees" tab (`SurchargeFeesPanel.tsx`, `SurchargeRuleFormDialog.tsx`); a lane's details page shows which surcharge rules apply to it; the settlement flow (`PostTripSettlementModal.tsx` and `TripDetailsPage.tsx`'s own settlement UI — there were two independent copies, both updated) now builds an itemized charge list (`TripChargeLineEditor.tsx`) instead of two flat number inputs. Collapsed three independent copies of "trip total = billing + extra charges" (two in `invoiceController.ts`, one in `reportsController.ts`) into `utils/tripFinancials.ts`. **Migration note**: the old two `Trip` columns are still in the schema (not yet dropped) — this repo deploys via `prisma db push`, which runs no data migration, so `scripts/backfill-trip-charges.ts` (`npm run backfill:trip-charges` / `:verify`) must copy their historical values into `TripCharge` rows in every environment before a follow-up change drops the columns. No real customer surcharge rates have been entered yet — the exact per-customer rates/units are still being confirmed with the client | ✅ mechanism (2026-08-17), 🔜 column drop + real rate data |
| **Bulk-import body limit + template discoverability fixes**: `express.json()` defaulted to 100kb, so importing a few hundred rows (e.g. the 652-row rate card sheet) failed with "request entity too large" before any row was processed — raised to 10mb. The Excel-template download link only appeared after a failed parse; `ExcelImportDialog` now also shows it up front, before any file is picked | ✅ (2026-08-11) |
| `/locations` CRUD (Admin+Operator): create-or-revive by slug, rename cascades into rate cards' denormalised names, delete refused while a live rate card prices it | ✅ (2026-08-09) |
| `POST /drivers/import` + `POST /vehicles/import` — upsert on phone / plate (never duplicates, never writes status), two-pass so cross-sheet assignments resolve, per-row errors and warnings | ✅ (2026-08-09) |
| `GET /trips/monthly` — a whole month grouped customer → day for the monthly-commitment board: per-company driver/vehicle/category rosters + month summary (trips, companies, distinct drivers/vehicles, value, trips missing a driver or truck). Day = `planned_start ?? createdAt` (same rule as the ledger's date filter); tier/category filters match the trip's own column **or** its rate card's, so pre-column trips aren't hidden. Capped at 5000 with a `truncated` flag | ✅ (2026-08-12) |
| `PATCH /trips/:id/stops/:stopId` — correct a stop's label, address, lane endpoint and pin. Allowed in flight, 409 once Completed/Invoiced/Cancelled; never touches arrival/departure timestamps | ✅ (2026-08-09) |
| `/locations` list returns `_count` of rate cards (both lane ends) and trip stops, so the Locations page can separate places in use from typos and orphans | ✅ (2026-08-09) |
| `TripStop.location_address` + `Location.address`; `mobileTripController` uses one shared `tripInclude` so every driver endpoint returns the stop's Location (some used `stops: true`, which silently omitted it) | ✅ (2026-08-09) |
| `POST /rate-cards/:id/assign` — copy a lane's price to N customers, skipping (never overwriting) customers who already price that lane | ✅ (2026-08-09) |
| JWT auth + **RBAC** `authorizeRoles` on all feature routes | ✅ (`ddbe688`) |
| Exactly 3 roles: Admin / Operator / Driver (Prisma enum + shared-types) | ✅ locked |
| Live GPS relay (Socket.io: receives `driver:location_update`, broadcasts to web) | ✅ |
| ICCES vehicle-tracker polling (3 background jobs) | ✅ (creds not provided yet) |
| File uploads (photos, documents) | ✅ |
| Zod request validation on write + list routes | ✅ (`79d4cd4`) |
| `POST /trips` requires `driver_id` + `vehicle_id` (no driverless trips); creates directly as `Dispatched` (same availability checks + `OnTrip` flip + driver notification as `dispatchTrip`), 400s if driver/vehicle isn't `Available` | ✅ (2026-07-28) |
| Structured logging (Pino), collision-safe reference IDs | ✅ (`d816736`, `f32cbca`) |
| `JWT_SECRET` rotated → GitHub Actions secret, leaked fallback removed | ✅ (2026-07-12) |
| Driver fixes: validate `PATCH /drivers/:id` (fixes "Failed to update"), free phone number on delete so it can be reused | ✅ (`a217c59`, `029cee7`) |
| **All 15 money columns converted `Float` → `Decimal(12,2)`** (`schema.prisma`, migration `20260820200436_money_columns_to_decimal`, **not yet deployed anywhere — awaiting the owner's go-ahead given the same day's dev.mercon.tech incident**): `Trip.billing_amount`/`trip_charges`/`third_party_cost`/`waiting_labor_charges`/`additional_stop_charges`, `Invoice.subtotal`/`total_amount`, `Expense.amount`, `Customer.credit_limit`, `MaintenanceRecord.cost`, `RateCard.base_price`/`default_trip_charge`, `SurchargeRule.rate`, `TripCharge.rate`/`amount`. Floats accumulate rounding drift on repeated updates and can't represent most decimal fractions exactly — a real exposure once `Invoice.zatca_ref` is used for actual ZATCA e-invoicing, which needs exact 2dp figures. Prisma's `Decimal` doesn't behave like `number` in JS: `0 + decimal` silently does STRING CONCATENATION (not addition — `Decimal.valueOf()` returns a string), `typeof decimal === 'number'` is false, and `JSON.stringify` emits a quoted string. Handled two ways: (1) `Prisma.Decimal.prototype.toJSON` overridden once in `src/db.ts` so any Decimal that reaches `res.json()` unmodified still serializes as a JSON number — deliberately NOT a Prisma Client `$extends` wrapper, which was tried first and reverted: it changes `prisma`'s TS type in a way that breaks every function typed to take a plain `PrismaClient`/`Prisma.TransactionClient` (`rateLookup.ts`, `locationController.ts`, `mobileTripController.ts`); (2) every in-process arithmetic site was audited and fixed by hand — `tsc` caught most (`0 + Decimal` fails to compile against the real Decimal type), two more were found by grep because the surrounding code was implicitly `any` (`reportsController.ts`'s and `tripReportData.ts`'s duplicate trip-billing calculators). The sharpest one: `tripController.ts`'s post-trip-settlement invoice recalculation (`baseBilling + computeTripChargesTotal(...)`) would have silently written a string-concatenated total onto a real invoice the first time it ran post-migration — verified fixed end-to-end against the real API (`1250.75 + 175.50` → invoice `total_amount: 1426.25`, a plain JSON number, not `"1250.75100.5075.00"`). Also fixed in the same pass: `resolver.ts`'s generic Report Builder field reader, which would have silently reported every money-typed "aggregatable" column as zero across the whole feature (every consumer downstream filters on `typeof v === 'number'`, which a raw Decimal fails). Verified: `tsc --noEmit` clean, `npm run test:xlsx` (17/17) unaffected, `migrate diff --from-migrations` against a shadow database shows the full local migration history reproduces `schema.prisma` exactly, realistic financial data created/read/settled through the live local API with hand-computed expected totals matching exactly | ✅ backend + migration written and verified locally, 🔜 not deployed |
| **Deploys now run `prisma migrate deploy`, not `db push`** (`backend/api-server/Dockerfile`, `prisma/migrations/`): the container used to start with `prisma db push --accept-data-loss || true` — no schema history to review or roll back, destructive changes applied unattended on every restart, and a swallowed exit code that let the API serve traffic against a database that did not match the code. The schema step is now fatal; the seed stays non-fatal but logs a warning. `0_init` was generated from the schema as it stood on `main`, so it describes what production already has; `20260821000000_indexes_and_location_codes` is the additive delta (17 indexes, `Location.codes`, 9 nullable `Customer` columns). Verified with `migrate diff --from-migrations` against a shadow database: the pair reproduces `schema.prisma` exactly, no drift. **⚠️ Production must be baselined once with `migrate resolve --applied 0_init` before `main` next deploys** — full procedure in `prisma/migrations/README.md`. **Incident, caught and fixed same session:** `dev.mercon.tech` went down (502, `dev-api` crash-looping on `Error: P3005`) minutes after this shipped to the `dev` branch, because `dev` auto-deploys on every push and had never been baselined either — the original note here only warned about production. Recovery needed a second step beyond the plain baseline: `dev-postgres` had drifted ahead of `0_init` (it tracked the `dev` branch's schema via `db push` the whole time, so it already had 9 `Customer` columns `0_init` doesn't know about), so the delta migration failed on a column collision until it was rolled back and the genuinely missing pieces applied by hand. `dev` fully recovered (verified `migrate status` → up to date, `/api/health` → 200); production was never touched or affected. `prisma/migrations/README.md` now documents both lessons | ✅ (2026-08-21) |
| **Foreign-key and soft-delete indexes added** (`schema.prisma`, owner-approved 2026-08-20): Postgres does not index foreign keys automatically and Prisma had not declared them, so — confirmed against the live database — `TripStop.tripId`, `Document(entity_type, entity_id)`, `Notification.userId`/`driverId`, `Invoice.customerId`/`tripId`, and `deletedAt` on Customer / Folder / Location / RateCard / ThirdPartyProvider were all unindexed. Every driver-or-vehicle document folder, every trip list that renders stops, and every notification poll was a sequential scan that got slower as the tables grew. 17 indexes now exist; `prisma db push` created them in 432 ms, so the deploy cost is negligible | ✅ (2026-08-20) |
| **Payload-shape fixes (round 3)**: `GET /documents`, `getOwnerFolder`, and the document lists embedded in the driver / vehicle / maintenance detail endpoints all returned the full `Document` row — including `ocr_raw_text` (the whole OCR dump of the scanned page, which nothing in either client reads) and `ai_extracted_json` (rendered only by `DocumentPreviewSheet`, which fetches its own document by id). All five now use a shared `utils/documentSelect.ts` allowlist; `GET /documents/:id` still returns the full row, so the preview sheet is unchanged. New `mode=lookup` on `GET /drivers/:id` and `GET /vehicles/:id` returns the owner without their trip history — the driver and vehicle **documents** pages were loading every non-cancelled trip that driver/vehicle has ever run, each with its customer, vehicle and full stop list, to render a name and a status badge | ✅ (2026-08-20) |
| **List-endpoint aggregation fixes (round 2)**: `getThirdPartyProviders` ran a `count` + an `aggregate` per provider inside a `.map()` — the providers page asks for `per_page=1000`, so one screen fired ~2000 queries; replaced with two `groupBy` calls, and the per-row `_count.trips` now excludes soft-deleted trips so it agrees with `total_cost`/`total_revenue`, which always did. `getExpenses` read every non-deleted expense row into Node and reduced over it to produce four KPI numbers — on every page of every request, including each keystroke of a search; now a single `groupBy(status, category)`. `GET /invoices` shipped whole `customer` and `trip` rows per invoice (`include: { customer: true, trip: true }`) for a list that renders a name and a trip ref — narrowed to the fields the declared `Invoice` type actually has. New `mode=lookup` on `GET /customers` (scalars, no per-row trip-count subquery). New `GET /third-party-providers/stats` for the 3PL KPI strip | ✅ (2026-08-20) |
| **List-endpoint payload/latency fixes**: gzip (`compression`) is now applied to every response — the VPS nginx only gzips `text/html`, so list JSON was going over the wire uncompressed (a fleet lookup measures 11.1 KB raw → 1.45 KB gzipped). `mode=lookup` on `GET /drivers` and `GET /vehicles` was widened from 4-5 columns to the full "picker" shape (every scalar a dropdown or export column reads, plus a shallow `assignedVehicle`/`assignedDriver` join) so the ~20 screens that fetch 100-1000 rows for a dropdown can stop asking for the default shape, which includes every in-progress trip per row with its own nested vehicle/driver/customer/stops. New `GET /drivers/stats` + `GET /vehicles/stats` return `groupBy` status counts for the roster/fleet KPI cards, replacing a `per_page=1000` fetch of that heavy shape that ran on every mount of those pages | ✅ (2026-08-20) |
| **Search normalisation** (`utils/search.ts` → `buildSearchAnd`): list endpoints (trips, drivers, vehicles, customers, invoices, maintenance, locations) split the query into words and require every word to match at least one searched field, case-insensitively, with punctuation bridged ("drv0001" finds `DRV-0001`). Driver search covers the full name, ref id, phone, licence and assigned plate (it only searched `first_name` before) | ✅ (2026-08-11) |

### Delay reporting (backend + web) — new 2026-08-05
| Piece | State |
|---|---|
| `TripStop.actual_departure` + `stampStopTransition()` owning the stop clock; every status path (web generic, pickupArrive, pickupVerify, mobile/geofence, completion) stamps arrival/departure inside its transaction | ✅ |
| Fixed: mobile/geofence path never wrote `actual_arrival`; dropoff arrival no longer backfilled at completion (it dated deliveries to when paperwork finished) | ✅ |
| Fixed: mobile operator Create Trip sent no `planned_arrival`, so phone-booked trips could never count as late | ✅ |
| `TripStop.location_name` + capture in both create-trip forms (web picker auto-fills from its existing Nominatim search, editable) | ✅ |
| `DelayReason` enum + `delay_reason`/`delay_note`/`delay_logged_by`/`delay_logged_at`; `PATCH /trips/:id/stops/:stopId/delay` (operator-only, re-loggable) | ✅ |
| Operator alert broadcast to every active Admin + Operator when an arrival lands 30+ min late; fires post-commit, once per stop | ✅ |
| `GET /reports/delays`, `/delays/grid`, `/delays/analysis` — shared filters (range, customer, driver, vehicle, reason); grid switchable by route/driver/company/vehicle; analysis at day→year granularity with previous-window deltas | ✅ |
| `/reports/delays` page: log (click-through to trip), on-time grid, analysis (KPIs, trend, 4 leaderboards), CSV export | ✅ |
| Reason picker on `TripDetailsPage` for any stop over the threshold | ✅ |
| Demo dataset for exercising the report: `scripts/seed-demo-local.ts` (~3,100 trips / 2 years, patterned so the report has real findings) + `scripts/demo-purge.ts` (deletes only rows tagged with the marker in `created_by`). Wiping requires `DEMO_RESET=yes` explicitly — never inferred from the hostname, since production's `postgres-db` compose service name is identical on a laptop (`455ff5b`) | ✅ |
| `.github/workflows/demo-data.yml` — `workflow_dispatch`-only seed/purge of the **hosted** database, gated on a typed `SEED`/`PURGE` confirmation and the `demo-data` GitHub Environment; runs both scripts via `docker exec` inside `mercon-api`, never passes `DEMO_RESET`, prints before/after row counts. Docs: `docs/LOCAL_DEMO.md` | ✅ (2026-08-07, unrun — needs the self-hosted runner) |

**Verification:** end-to-end over real HTTP against a live Postgres — operator creates a trip,
driver runs it late through the mobile endpoints, operators get alerted, an operator logs the
reason, and every report view reflects it; plus invoice generation and driver/vehicle release
confirmed unbroken, and a driver token confirmed rejected from the operator reports. The report
page was also walked in a real browser against a seeded dataset (3 companies, 4 routes, 25 trips),
which caught two render bugs a type-check could not (percentage-height bars collapsing to zero,
and the ﷼ glyph reordering beside digits).

**Not covered:** no independent GPS cross-check of departure times — the client's reference sheet
compares three sources, MERCON has one (the driver's app) until ICCES credentials arrive. Waiting
time is measured and reported but never billed automatically. Alerts reach open dashboards, not
locked phones (needs push, still deferred). History starts at deploy: older trips have no location
names, unreliable arrivals and no reasons, and cannot be backfilled.

### Web dashboard (`frontend/web-dashboard`) — Admin + Operator
| Piece | State |
|---|---|
| **Zero-Configuration Company Reports & Layout Mapping**: Eliminated manual column mapping dropdown configurations. Operators upload a target Excel layout template (.xlsx); the system dynamically inspects column headers using normalized fuzzy logic (e.g. matching fields like Trip ID, Driver, Rate), preserves original cell styles/fonts/formulas, and exports populated sheets on download. Upgraded the frontend Company Reports page layout to a side-by-side split layout: directory tree cards and config filters panel (left) and KPI statistics preview with matching trip record DataTable ledger (right), with interactive mapping review & override editor during upload, permanent Edit Mapping support in the ledger header, and template layout versioning. | ✅ (2026-08-24) |
| **Static assets are now compressed and cached correctly** (`frontend/web-dashboard/nginx.conf`): the container serving the dashboard had nginx's default `gzip off`, so ~6 MB of built JS/CSS was downloaded uncompressed on every cold load — gzipped it is ~1.7 MB. Hashed asset filenames now get `immutable` + 1-year caching (they were 30 days with revalidation), and `index.html` is explicitly `no-cache`, since it is the pointer naming the current bundle hashes and a cached copy keeps sending browsers to the previous deploy's files | ✅ (2026-08-20) |
| **Mount-time mega-fetches removed (round 2)**: the Trips page fetched 1000 trips (each with stops, driver, vehicle, customer and rate card) on every mount under the name `trips-kpi-summary` — it no longer fed any KPI card, only the export sheet, so it is now `enabled: isCustomExportOpen` (and its failure toast reworded, since it was claiming stats had failed). The 3PL page's 1000-provider KPI fetch is replaced by `GET /third-party-providers/stats`. Eleven customer pickers now pass `mode: 'lookup'`. `placeholderData: keepPreviousData` extended to the Customers, Rate Cards, 3PL and Trips lists. Separately, four customer pickers (Upload Document, Documents Center, Create Invoice, Company Reports) called `customerService.getAll()` with no `per_page`, so they silently offered only the first 20 customers — now 500 in lookup shape | ✅ (2026-08-20) |
| **Slow dropdown / first-search fixes**: the driver picker on Add Vehicle (and ~20 similar pickers) fetched its list from the default `GET /drivers`/`GET /vehicles` shape at `per_page` 100-500 — every row carrying its in-progress trips with nested vehicle/driver/customer/stops — which is why "Assign Driver" could take a minute to populate. All picker queries now pass `mode: 'lookup'`. Driver/Vehicle list pages no longer fetch 1000 rows of that shape on mount for their KPI cards: counts come from `GET /drivers/stats` / `GET /vehicles/stats`, and the full roster is fetched only when the export sheet or the expired-licence drill-down actually opens (`enabled:`), in `lookup` shape — that mount-time request was what the first search typed into the bar was queuing behind. Both list queries got `placeholderData: keepPreviousData` so a search keeps the previous rows on screen instead of collapsing to a skeleton. Also fixed a cache-key collision: `CreateDriverModal` cached its Available-only fleet under the same `['vehicles-select']` key four other screens use for the unfiltered fleet, so whichever loaded first won | ✅ (2026-08-20) |
| **Paste a Google Maps link to set a location**: any address search box now recognises a pasted Maps URL and resolves it to a pin instead of running it through Places autocomplete (which never matched a URL). Full links (`maps.google.com/maps?q=lat,lng`, `/maps/@lat,lng,17z`, `/maps/place/…!3d…!4d…`) are parsed client-side in `utils/googleMapsLink.ts`; short links (`maps.app.goo.gl/…`, `goo.gl`, `g.co`) carry no coordinates and can't be expanded in-browser (a cross-origin redirect is opaque to `fetch`), so they go through `GET /api/geocoding/resolve-maps-link`, which follows the redirect server-side and is host-allowlisted so it can't be used as an open proxy. Coordinates are reverse-geocoded (Nominatim, already in the codebase) to fill the name/address. Wired into `TripLocationField` (Create Trip), `LocationPickerMap` (Edit Trip stops via `StopAddressEditor`), `LocationFormDialog` (`/locations` add/edit) and `AddSavedLocationDialog` (customer saved places) | ✅ (2026-08-20) |
| **Documents Center redesign** (owner-first document model): `DocumentType` (DB-driven mandatory/optional config, seeded with Mercon's 4 driver + 5 vehicle requirements, editable at `/settings/document-types`, Admin-only) + `DocumentFile` (multi-file) Prisma models, additive-only schema change; existing documents backfilled with no data loss. `GET /documents/owner-folder` + `documentStatusService` centralize compliance status. `OwnerFolderDetail` shows a Driver's/Vehicle's full checklist incl. undocumented mandatory types ("Missing"); reused by `DriverDocumentsPage`/`VehicleDocumentsPage` (rebuilt) and Documents Center folder cards. Upload is file-first; in-folder uploads already know the owner. "Create Folder" splits into Owner Folder (opens the existing owner folder, no new record) vs General Folder (unchanged, for unowned Company/Ops docs) | ✅ (2026-08-17) |
| **Admin Dashboard** (`/Ilan`, Admin-only, unlisted — no sidebar entry, reachable only by direct URL — `AdminDashboardPage.tsx` + `components/dashboard/{ActiveTripsWidget,LaborChargeQueueWidget,TopCustomersWidget,ActionsNeededWidget}.tsx`): live active-trips list, post-trip settlement queue, top customers, and an "Action Center" — separate from the main `/` dashboard, which is unchanged. KPI strip matches the Vehicles page's instrument-panel cards (`components/ui/KpiRouteFooter.tsx` for the two vehicle-concept cards; Settlement Queue/Revenue get their own honest footers — an aging bar and a sparkline — instead of a reskinned truck). Actions Needed is a unified, urgency-sorted, filterable list across 6 categories (documents, maintenance, delays, invoices, rate cards, locations); delay items open `components/trips/LogDelayReasonModal.tsx`, the first working UI anywhere in the app for `tripService.logStopDelay`. Top Customers rows now link to `/customers/:id`, and show real credit-exposure and active-trip signal per customer (backend `getRevenueReport` now returns each top customer's `id`, not just `name`/`value`) | ✅ (2026-08-11) |
| 46 pages, all wired to the real API (no mock data left) | ✅ |
| **Excel import** on Drivers/Vehicles: `importUtils.ts` (content-located header row, loose header matching, Date/formula/rich-text flattening) + shared `ExcelImportDialog` (preview → confirm → created/updated/failed). Fixed two template bugs found while verifying: an over-31-char sheet name that made the vehicles workbook unopenable, and a generator writing to an unserved `frontend/public` | ✅ (2026-08-09) |
| Trip Details shows each stop's full address (amber "no address — the driver only gets a map pin" when absent); Edit Trip gained a per-stop editor (`StopAddressEditor`) — previously a wrong address could never be corrected after dispatch | ✅ (2026-08-09) |
| **Monthly Trips by Company** (`/trips/monthly`): one **command bar** card (identity + month stepper + icon-grouped refresh/export + New Trip, then search and all four filters joined into a single segmented control with internal hairline dividers, then a result-context line and removable filter chips) over a `xl:grid-cols-2` grid of company cards, each holding a tight `Table` of that company's trips — deliberately just date, driver, vehicle, status; route/category/amount live in the row-click detail dialog rather than cluttering the row. Owner feedback removed/changed, in order: a page-level 6-tile KPI strip, a per-company Calendar/Schedule toggle, a per-company rainbow colour system (`companyHue`) called out as generic, an intermediate vertical field-grid layout that produced too much whitespace/scrolling, the single full-width column of cards (confirmed via `AskUserQuestion` that "vertically stacked" meant cards side by side), the table's own column count, and finally the three detached floating boxes (header / search / filters) that became the one command bar | ✅ (2026-08-12) |
| **Locations page** (`/locations`): usage counts, missing-address banner, unused/incomplete filters, rename-cascades-to-rate-cards, delete blocked by the API while a rate card prices the place. No merge for two spellings that both already exist — still a manual re-point | ✅ (2026-08-09) |
| 12 service modules (auth, customer, driver, invoice, location, notification, rateCard, reports, trip, user, vehicle, document) | ✅ |
| **Rate cards rebuilt around lanes**: `LocationCombobox` (picks a place, adds one inline from typed text), shared `RateCardFormDialog` with an explicit standard-vs-one-customer choice (the old create form *required* a customer, so standard lanes were unreachable), `AssignRateCardDialog`, "Not linked" flags on cards that still carry pre-lane free text | ✅ (2026-08-09) |
| **Rate card Excel import**: `RateCard.vehicle_type`/`rate_category`/`via_location` (free text — no fixed vehicle-type enum, quotes name their own tiers), `POST /rate-cards/import` (flat sheet, customer must pre-exist, "Surcharge" category for flat fees with no lane), "Import Excel" on the Rate Cards list via shared `ExcelImportDialog`. Owner's actual 7-carrier quotation workbook converted to a ready-to-import file for review, not yet imported. `MERCON_Customers_ReadyToImport_Carriers.xlsx` ships the 7 carrier names for the required prerequisite Customer import (phone left blank — not in the source file) | ✅ (2026-08-11) |
| **Rate Cards ledger filters by Vehicle Type / Rate Category**: the ledger already showed each card's tier as a badge but had no way to filter by it — added `vehicle_type`/`rate_category` as `GET /rate-cards` query params (`rateCardController.getRateCards`) and two dropdowns (sourced from `VEHICLE_TYPES`/`RATE_CATEGORIES`) to `RateCardListPage.tsx`, alongside the existing Status filter | ✅ (2026-08-12) |
| **Trip wizard picks the right rate tier, not an arbitrary one**: a lane can now have several `RateCard` rows (imported tiers differing by `vehicle_type`/`rate_category`) where before there was always at most one per customer+lane. `CreateTripPage.tsx` (full-page wizard) used to call `GET /rate-cards/lookup`, which does `findFirst` ordered by `updatedAt desc` — with multiple tiers that silently returns whichever was touched last, not the right one. Rebuilt to match `CreateTripModal.tsx`'s existing pattern: fetch every card for the lane (`GET /rate-cards` with lane+customer+`include_standard`), auto-select the customer's own card or the standard one, and show a tier-picker (`rate_category` · `vehicle_type` chips) when more than one exists. `TripStepRatesBilling.tsx` (used by the modal) also gained the same tier badges, which it didn't show before. Mobile's operator Create Trip screen doesn't touch rate cards at all, so no equivalent gap there | ✅ (2026-08-11) |
| **Create Trip step 3** is a two-column grid of `TripStopCard` — one card per stop: pricing hub (Origin/Destination location) → exact point → planned time. The map and the manual name/address fields fold away behind "Adjust pin" / "Edit" (`LocationPickerMap` `compact` mode); the address search alone covers the common case. Pin + stop name still pre-fill from the chosen place and stay editable. The rate is looked up server-side per customer+lane, the matched tier is shown, and a typed price for an unpriced lane can be saved as standard / this-customer-only / not at all | ✅ (2026-08-10) |
| Customer details page lists the customer's own rates plus the standard lanes they inherit (overridden lanes suppressed), with add/edit/override inline — replaces a block that rendered `origin_city`/`destination_city`/`asset_type`, none of which exist on a rate card | ✅ (2026-08-09) |
| Live trip tracking over WebSocket | ✅ |
| Role-gated pages (`RequireRole`) + User Management (Admin + Operator; lists Admin/Operator web users plus Drivers read-only, "Add Driver" links to `/drivers/new`) | ✅ |
| Operator-driven password reset + "notify my operator" flow | ✅ |
| Debounced server-side search across list pages | ✅ (`0546e0f`) |
| **Normalised search** (`lib/search.ts` → `matchesSearch`): every browser-filtered list and picker (Documents Center, Expiry, Payment Status, Rate Cards, Locations, Users, Notifications, Recycle Bin, Vehicle Financials, Fleet map, comboboxes) matches word-by-word, ignoring case, accents, Arabic-Indic digits and separators — "ksa1029" finds `KSA-1029`, "smith john" finds "John Smith" | ✅ (2026-08-11) |
| Brand/semantic color tokens as Tailwind utilities | ✅ (`12fa35b`) |
| Site-wide semi-curved corners (replaced 224 `rounded-none` overrides → `rounded-lg`; shadcn primitives use idiomatic radii) | ✅ |
| Create Trip: driver + vehicle now required (no "leave unassigned"); pickup/dropoff lat/lng number inputs replaced with `LocationPickerMap` (address search via OpenStreetMap Nominatim + click/drag pin on a Leaflet map, no API key) | ✅ (2026-07-28) |
| Login page redesign: full-bleed logistics background image (`login-bg.png`), no center divider, logo pinned top-right, centered "Welcome back" heading + boxed sign-in card | ✅ (PR #2 → `a09fa8d`) |
| Fleet Excel **export**: Drivers/Vehicles list pages export a real `.xlsx` (upgraded 2026-08-08 from the earlier HTML-as-`.xls` trick — see `exportUtils.ts` note below) including the driver's **assigned vehicle plate** and the vehicle's **assigned driver**, resolved from the active trip (`Dispatched`/`AtPickup`/`InTransit`/`AtDelivery`) that `getDrivers`/`getVehicles` now `include` | ✅ (2026-08-04, `234d191`; restyled 2026-08-08) |
| **Mobile responsiveness**: sidebar is now an off-canvas drawer below `lg` (backdrop, Esc/route-change/nav-tap close, body-scroll lock) with a hamburger in the header; the 8-item route pill bar is hidden below `lg` and Quick Create moved to a header `+` button; `DataTable` scrolls horizontally (`min-w-[720px]`) with a responsive toolbar/footer; `dialog.tsx` gets viewport margin + `max-h-[90dvh]` scroll so every modal fits a phone; page containers `px-4 sm:px-6`; unprefixed `grid-cols-2/3/4` given mobile-first breakpoints | ✅ (2026-08-05) |

### Mobile app (`frontend/mobile-app/mercon-app`) — Expo, Driver + Operator
| Piece | State |
|---|---|
| App entry fixed (`expo-router/entry`), 24 screens type-check (tsc 349→0) | ✅ |
| Runtime packages installed (axios, socket.io-client, expo-secure-store, expo-location, expo-image-picker) | ✅ |
| API client (JWT interceptor) + SecureStore auth context (auto-login) | ✅ |
| **Unified login** (single form, auto-detects driver vs operator by credentials — no mode toggle) + **role routing** (`app/index.tsx`); redesigned UI (real logo, hero background image `login-hero.png`, simplified Username/Password inputs — driver: phone/license, no welcome heading, centered compact card, notify-operator button below card) | ✅ (`c3c83cf`) |
| Driver **Home**: real current trip, status updates, cargo + POD photos (camera), photo-gated status | ✅ (`fee951d`, `dd184c1`) |
| **Driver sees where they're going**: `TripStop` type gained `location_name`/`location_address`/`location` (it declared only lat/lng, so the API's address was invisible to the app) + `stopLabel()`/`stopAddress()` helpers. Route timeline, navigation card, pickup/delivery verification and the trips list all show the place and its address instead of coordinates | ✅ (2026-08-09) |
| **Normalised search** (`lib/search.ts` → `matchesSearch`, its own copy of the dashboard helper since the app shares no package with it): the operator Trips/Fleet/Invoices lists and the driver Trips list match word-by-word, ignoring case, accents, Arabic-Indic digits and separators. The server-searched screens (operator Drivers, Customers) inherit the same behaviour from the API | ✅ (2026-08-11) |
| Bottom nav redesign (driver + operator): lucide icons, orange "capsule" active indicator (springs in on page switch), sized like a standard app bar, dropped lower | ✅ |
| Profile header redesign: larger avatar left, name + driver ID + status badge stacked to its right | ✅ |
| **App-wide emoji → lucide icon sweep**: every driver + operator screen, shared `SearchInput`, and the `docIcon`/`notificationIcon` helpers now use lucide icons (no emojis anywhere in the UI) | ✅ |
| Active-trip card redesign (Home): fixed edge-clipping (DarkCard padding) + route timeline, divider, tidy meta row | ✅ |
| Home screen: trip section (empty banner or active-trip card) now centers vertically in the remaining page space; full-screen faded truck/route background image (`home-bg.png`) behind header + content | ✅ |
| Photo upload: camera **or** gallery (`choosePhoto` chooser) on pickup/delivery/home; pickup checklist removed, larger confirm button | ✅ |
| **Operator Home screen rebuilt** (`features/dashboard`): first use of **NativeWind** (Tailwind for RN) and **TanStack Query** in the mobile app — `babel.config.js`/`metro.config.js`/`tailwind.config.js` added, `global.css` wired through `_layout.tsx`, root wrapped in `QueryClientProvider`. Screen: `AppHeader` + `ContextSelector` (role chip) + `SearchBar`/`ScannerButton`, two `DashboardMetricCard`s (Active Trips / Delayed Deliveries — "delayed" derived from `planned_end` vs. now, no backend status for it), `ActiveVehiclesSection` (snapping `FlatList` carousel of `VehicleCard`s — driver, illustrated `RoutePreview`, truck ID/model/capacity, a derived `VehicleCardStatus` pill), and `DocumentExpirySection` (**replaces** the old Fleet Utilization block — summary counts + All/Vehicles/Drivers/Company-scoped list of real documents with an `expiry_date`, "Company" meaning "not a Driver/Vehicle doc" since the backend has no Company entity). Layered api → services → hooks throughout; presentation components never call the API | ✅ (2026-08-05) |
| **Operator Drivers list screen rebuilt** (`features/drivers`): two dark `DriverStatCard` KPI tiles (Total Drivers/On Trip, with available/offline as captions), inline search, status `FilterBottomSheet`, `SortDropdown` (name/newest/most-trips/available/online — all client-side, since `GET /drivers` has no `sort_by` support), backend-paginated infinite-scroll `FlatList` of `DriverCard`s (avatar+status dot, status badge, assigned vehicle + coarse "En Route"/"Unknown" location, rating **"—"** and trip count from `GET /reports/drivers` joined client-side — no rating field exists in the schema, never fabricated). `useDrivers`/`useDriverStats`/`useDriverSearch`/`useDriverFilters`/`useDriverSorting` hooks | ✅ (2026-08-05) |
| **Operator Customers screen rebuilt** (`features/customers`, route `/operator/customers`, reached from More → Customers): dark KPI rail (total customers, active rate cards, trips this month, pending bills), server-side search + active/inactive filter, client-side sort, backend-paginated infinite scroll, and per-customer trips/revenue/outstanding/rate-card derived by aggregating `/trips`, `/invoices` and `/rate-cards` once each (the customer table joins nothing, so this avoids an N+1). Overflow menu gates Edit/Suspend/Delete off the authenticated role; suspend + delete are optimistic across every cached page with rollback. Also extracted a generic `SortDropdown` and `useDebouncedValue` into `shared/` (drivers now use both instead of near-duplicates), and `create-trip` accepts a `customerId` param so "Create Trip" pre-selects the customer | ✅ (2026-08-05) |
| **Operator Driver Details screen** (`features/drivers`, route `/operator/driver-details?id=`): profile card + statistics, assigned vehicle (dark card, from the active trip), contact/licence info, horizontal documents list with derived Valid/Expires Soon/Expired/Pending Renewal status, current assignment, performance summary, call/message actions. Layered api → services → hooks (`useDriver`, `useDriverDocuments`, `useDriverPerformance`, `useDriverAssignment`, `useDriverActions`) with React Query caching, pull-to-refresh, 60s auto-refresh and optimistic status updates; presentation components never call the API. Driver list now taps through to it instead of the edit form | ✅ (2026-08-05) |
| Driver trip flow now goes straight through, no detour back to Home: pickup cargo photo → real live map (`react-native-maps` + OSM tiles, no API key) with the dropoff pin → GPS geofence auto-detects arrival (200m) and jumps straight to POD → complete. `DestinationReachedScreen` (manual "confirm arrival" screen) removed; `LiveNavigationScreen` replaced (was a fake placeholder) and wired at `/trip/navigate` | ✅ (2026-07-28) |

### Fleet data onboarding (Excel templates + guide)
| Piece | State |
|---|---|
| `scripts/generate_excel_templates.py` — generates styled bulk-entry workbooks (banner, info cards, dropdown validation, sample rows) | ✅ |
| `MERCON_Drivers_Import_Template.xlsx` / `MERCON_Vehicles_Import_Template.xlsx` in `docs/templates/` + `frontend/public/templates/` (downloadable) | ✅ |
| Drivers sheet columns aligned to the real Prisma model + an `Assigned Vehicle Plate` column (dropped the speculative Email / Emergency-Contact columns — no schema fields for them) | ✅ (2026-08-04) |
| Vehicles sheet: ICCES device ID + `Assigned Driver Phone / Name` column | ✅ (2026-08-04) |
| `docs/MERCON_Fleet_Import_Guide.md` — column specs, validation rules, Prisma field mapping, owner instructions | ✅ |
| **Backend import endpoint (parse an uploaded workbook → create drivers/vehicles)** | ❌ **does not exist** — see §3 |

> ⚠️ Honest scope: these templates are a **manual data-collection format** the owner sends to the
> client. Nothing in the app ingests them yet — the filled workbook has to be entered by hand or
> imported by a future endpoint. The guide's "API Import Schema Mapping" section is a *spec for
> that future endpoint*, not a description of shipped code.

### Mobile backend endpoints (`/api/mobile/*`)
| Endpoint | State |
|---|---|
| `POST /mobile/login` (unified, returns role) | ✅ |
| `GET /mobile/trips/current` | ✅ |
| `POST /mobile/trips/:id/status` | ✅ |
| `POST /mobile/trips/:id/photo` | ✅ |
| `GET /mobile/trips/history` (past trips, `?limit`) | ✅ |
| `GET /mobile/notifications` + `POST /:id/read` (driver, keyed by `Notification.driverId`) | ✅ |
| Driver "Trip Assignment" notification on dispatch + driver-replace | ✅ delivers |
| `GET /mobile/profile` (identity, license, phone, active-trip vehicle) | ✅ |
| `POST /mobile/emergency` (alerts all active Admins + Operators) | ✅ |
| `GET /mobile/documents` (driver's own docs) + `GET /mobile/vehicle` (active-trip vehicle) | ✅ |

---

## 3. 🔜 What's next (remaining work)

Legend: ⬜ not started · 🔄 in progress · ✅ done

### Surcharge/trip-charge follow-up (blocked on client, then a schema PR)
- ⬜ Get the client's confirmed per-customer surcharge rates and units (per-hour vs per-person vs flat, real current AKS additional-stop rate vs. the quotation sheet's stale 100/stop, what HORIZON's 3rd-party rows actually represent) and enter them as `SurchargeRule` rows via the new Surcharge Fees tab — no real rates seeded yet.
- ⬜ Run `npm run backfill:trip-charges -w @mercon/api-server` then `:verify` against every environment (dev, staging, production) to confirm `waiting_labor_charges`/`additional_stop_charges` migrated cleanly into `TripCharge` rows.
- ⬜ Follow-up schema change: drop `Trip.waiting_labor_charges`/`additional_stop_charges` from `schema.prisma` once every environment is backfilled and verified — this is a separate, destructive `db push`, not part of the mechanism already shipped.

### Milestone 1 — Finish driver trip workflow
**Backend endpoints (missing):**
- ✅ `GET /mobile/trips/history` — driver's past trips
- ✅ `GET /mobile/notifications` (+ `POST /:id/read`) — driver notifications
- ✅ `POST /mobile/emergency` — emergency alert (notifies operators/admins)
- ✅ `GET /mobile/profile` — driver profile

**Wire driver screens (currently static UI):**
- ✅ `PickupVerificationScreen` → cargo photo + `AtPickup → InTransit` (`/trip/pickup`)
- ✅ `DeliveryVerificationScreen` → POD photo + `AtDelivery → Completed` (`/trip/delivery`); receiver-name/signature dropped (no schema field — owner decision)
- ✅ `LiveNavigationScreen` → real map + GPS geofence auto-detects `InTransit → AtDelivery` (expo-router `/trip/navigate`, replaces the old manual `DestinationReachedScreen`/`/trip/arrived`)
- ✅ `TripCompletedScreen` → real summary of the latest completed trip (`/trip/completed`)
- ✅ `TripsScreen` (history) — real Active/Upcoming/Completed tabs
- ✅ `NotificationsScreen` — real feed + mark read / mark all
- ✅ `ProfileScreen` — real identity/license/vehicle + working logout
- ✅ `DocumentsScreen` → `GET /mobile/documents` (real docs, expiry status, open file)
- ✅ `AssignedVehicleScreen` → `GET /mobile/vehicle` (active-trip vehicle + honest empty state)
- ✅ `EmergencyScreen` → `POST /mobile/emergency` (incident type + notes + GPS location now attached; reachable via SOS button on Home + Live Navigation — was previously unwired/unreachable); photo capture UI still local-only — backend has no attachment endpoint for emergency reports yet (needs a `DocType`/schema decision, flagged separately)
- ✅ `SettingsScreen` — real logout (toggles are local-only); reachable (`/settings`)
- ⬜ `ReplacementDriverScreen` / `SplashScreen` (as needed)

### Milestone 2 — Driver live GPS
- ✅ `expo-location` foreground tracking while a trip is active (~10s / 20m) — plugin added to `app.json`
- ✅ `socket.io-client` connect + emit `driver:location_update` (shared socket, backend receives it)
- ⬜ Handle background / locked screen / network drops (foreground done; background is a hardening step)
- 🔄 End-to-end: truck moves live on the operator's web map — code complete, needs real-device verification

**How it's wired:** `src/lib/socket.ts` (shared connection), `use-live-tracking.ts` (watch + emit),
`DriverLiveTracking.tsx` (headless, polls current trip) rendered on the driver landing. Streams
`{ tripId, driverId, lat, lng, speed }`; the web `TripTrackingPage` already listens on
`trip:location_update:<tripId>`.

### Milestone 3 — Operator mobile screens
- ✅ `_layout.tsx` shows `OperatorBottomNav` for both `Operator` and `Admin` roles (was Operator-only, so Admin got the driver nav — fixed)
- ✅ `src/app/operator/{trips,trip-details,create-trip,drivers,vehicles,invoices}.tsx` route files created; registered in the root `Stack` and `TAB_ROUTES`
- ✅ `OperatorBottomNav` — Trips/Drivers tabs and the FAB now navigate to real routes (previously Drivers→`/documents`, Trips→driver `/trips`, FAB did nothing)
- ✅ `HomeScreen` (dashboard metrics) — real KPIs via `/reports/summary` + active trips via `/trips`
- ✅ `TripListScreen` — real `/trips` with status filters, KPI chips, search; cards tap through to `TripDetailsScreen`
- ✅ `TripDetailsScreen` — real trip via `GET /trips/:id` (timeline built from status + stop timestamps, cargo, driver, vehicle); "Call Driver" opens the dialer; **real lifecycle actions** now replace the old "coming soon" stub: contextual next-step button per status (`POST /trips/:id/pickup/arrive` → `PATCH /trips/:id/status` InTransit/AtDelivery/Completed), "Replace Driver" (live available-drivers picker modal → `POST /trips/:id/replace-driver`), "Cancel Trip" (`PATCH .../status` Cancelled); "Track Live" still shows a "coming soon" alert (no map yet)
- ✅ `CreateTripScreen` — real customers (`GET /customers`) + available drivers/vehicles (`GET /drivers|vehicles?status=Available`); posts `POST /trips` with pickup/dropoff lat-lng (plain numeric inputs, no map picker yet); form fields now use the shared `Input`/`Card` components instead of raw `TextInput`
- ✅ `DriverListScreen` — real `/drivers` (status filters, search, tap-to-call); tapping a card now opens `DriverEditScreen` (`PATCH /drivers/:id` — name/phone/license/status)
- ✅ `VehicleListScreen` — real `/vehicles` (status filters, stats, search); tapping a card now opens `VehicleEditScreen` (`PATCH /vehicles/:id` — plate/capacity/asset type/status)
- ✅ `VehicleRenewalScreen` — real vehicle documents via `GET /documents?entity_type=Vehicle` joined with `GET /vehicles` for plate numbers; due/critical/overdue buckets derived from `expiry_date` (no fake doc types/costs — uses the real `DocType`/`DocStatus` enums); reachable from the Home KPI "documents expiring soon" alert and from the new "More" hub; "Renew Now" still shows a "coming soon" alert (no re-upload flow yet)
- ✅ `InvoiceListScreen` — real `/invoices` (status filters, stat cards, search); Pending/Overdue invoices now show a "Mark as Paid" action (`PATCH /invoices/:id/status`); fixed a real bug where `Paid`/`Draft`/`Overdue` were missing from the status-color map and silently rendered as gray "Pending"
- ✅ `CustomerListScreen` + `CustomerEditScreen` (new) — full customer list/search/active-filter via `GET /customers`, create via `POST /customers`, edit (incl. active/inactive toggle) via `PATCH /customers/:id`; reachable from the new "More" hub and its own "+" button
- ✅ `MoreScreen` (new, `/operator/more`) — fixes a real navigation gap: `OperatorBottomNav`'s "More" tab used to route straight to the **driver's** settings screen, meaning Vehicles/Invoices/Vehicle Renewals (and now Customers) had no in-app entry point at all, only reachable via a raw deep link. The "More" tab now opens this hub (Fleet/Invoices/Customers/Vehicle Renewals + sign out) first.
- ✅ Fixed a duplicate-bottom-nav bug: `OperatorBottomNav` is already rendered globally as a floating overlay in `_layout.tsx`; an earlier pass had also rendered it inside 5 individual screens, showing two nav bars stacked.
- ✅ UI-kit consistency pass across all 8 operator screens: swapped hand-rolled filter pills/cards/buttons for the shared `FilterChip`/`Card`/`Button`/`StatusBadge` components, replaced hardcoded hex colors with `theme/tokens.ts` values (several were off-palette drift, e.g. `#D97706`, `#FFF7ED`, `#1A1A1A` for the dark header).

### Milestone 3.5 — Fleet bulk import (owner onboarding)
- ✅ Styled Excel templates + import guide (drivers & vehicles, incl. assignment columns)
- ✅ Excel export from the web Drivers/Vehicles list pages (round-trips the same columns)
- ✅ `POST /drivers/import` / `POST /vehicles/import` — Zod-validated, upsert on phone / plate,
  two-pass so the cross-sheet assignment columns resolve, per-row errors **and** warnings
  (record imported, link skipped). Rows are parsed client-side with ExcelJS and posted as JSON,
  same contract as `/trips/bulk-import` — no upload handling or spreadsheet lib on the server
- ✅ Web UI: "Import Excel" on the Drivers/Vehicles list pages (drag-drop → preview → confirm →
  created/updated/failed summary)

### Milestone 3.5 — Delay reporting follow-ups
- ⬜ Push notifications so a delay alert reaches a phone that is locked / app closed (currently in-dashboard only)
- ⬜ Geofence/ICCES departure time as an independent second source, to match the client's three-way cross-check (blocked on credentials)
- ⬜ Optional: auto-flag dwell time past ~5h as chargeable (client rarely bills the first 1–2h, so this stays a manual operator decision for now)
- ⬜ Optional: reason capture on the operator mobile app (web-only today)

### Milestone 4 — Testing, builds, handover
- ⬜ Real-phone test, both roles (Android + iPhone)
- 🔄 EAS builds — APK (Android) + TestFlight (iOS); app linked to an EAS project (`c99aab6`), no build run yet
- ⬜ Database backup set up + tested once
- ⬜ Short user guide (operator + driver, with screenshots)
- ⬜ Full end-to-end acceptance: create trip → driver runs it → invoice appears

### Schema note — driver notifications (resolved 2026-07-25, Option A)
`Notification` now has a nullable `userId` **or** `driverId` recipient (added `driverId`,
made `userId` optional). Web notifications still key off `userId`; driver notifications
key off `driverId`. `createDriverNotification()` + the trip-assignment trigger use it.
**Schema change deploys via `prisma db push --accept-data-loss` — additive/widening, no data loss.**

### Deferred (not v1)
- Push notifications (FCM) · map upgrade (Google/Mapbox) · OTP SMS login · big automated test suite · ICCES live creds (env vars pending)

---

## 4. Screen wiring tracker (mobile)

| Screen | Role | Wired? |
|---|---|---|
| LoginScreen | shared | ✅ |
| HomeScreen (trip flow + photos) | driver | ✅ |
| TripsScreen (history + active/upcoming tabs) | driver | ✅ |
| NotificationsScreen (feed + mark read/all) | driver | ✅ |
| ProfileScreen (identity/license/vehicle + logout) | driver | ✅ |
| EmergencyScreen (alert → operators/admins) | driver | ✅ |
| LiveNavigationScreen (live map, GPS auto-arrival → AtDelivery) | driver | ✅ |
| PickupVerificationScreen (cargo photo → InTransit) | driver | ✅ |
| DeliveryVerificationScreen (POD photo → Completed) | driver | ✅ |
| TripCompletedScreen (completion summary) | driver | ✅ |
| SettingsScreen (logout) | driver | ✅ |
| DocumentsScreen (real docs + expiry) | driver | ✅ |
| AssignedVehicleScreen (active-trip vehicle) | driver | ✅ |
| ReplacementDriver/Splash | driver | ⬜ static (secondary) |
| HomeScreen (`features/dashboard`: header/context/search, 2 metric cards, Active Vehicles carousel, Document Expiry section — NativeWind + React Query) | operator | ✅ (2026-08-05) |
| TripListScreen (filters + KPI chips + search, tap→details) | operator | ✅ |
| TripDetailsScreen (real trip, timeline, call driver, lifecycle actions, replace driver, cancel) | operator | ✅ |
| CreateTripScreen (real customers/drivers/vehicles, POST /trips) | operator | ✅ |
| DriverListScreen (`features/drivers`: 2 KPI tiles, search/filter/sort, infinite scroll, tap→details) | operator | ✅ (2026-08-05) |
| DriverDetailsScreen (profile, assigned vehicle, contact, documents, current assignment, performance, call/message) | operator | ✅ (2026-08-05) |
| DriverEditScreen (PATCH /drivers/:id) | operator | ✅ |
| VehicleListScreen (filters + stats + search, tap→edit) | operator | ✅ |
| VehicleEditScreen (PATCH /vehicles/:id) | operator | ✅ |
| InvoiceListScreen (filters + stats + search, mark-as-paid) | operator | ✅ |
| VehicleRenewalScreen (real doc expiry tracker, reachable from Home alert + More hub) | operator | ✅ |
| CustomersScreen (`features/customers`: KPI rail, search/filter/sort, infinite scroll, per-customer trips/revenue/rate card, overflow menu with role-gated suspend/delete, Create Trip) | operator | ✅ (2026-08-05, rebuilt) |
| CustomerEditScreen (create + edit, PATCH/POST /customers) | operator | ✅ |
| MoreScreen (Fleet/Invoices/Customers/Renewals hub + sign out) | operator | ✅ |

**Wired: 28 / 31 screens** (all core driver screens + all operator screens except the secondary
driver Replacement/Splash screens). Operator nav now actually reaches every wired operator screen —
Admin included — including the ones previously stranded behind the driver settings "More" route.

**Driver navigation now works** (expo-router): the bottom nav (Home/Trips/Profile) and Profile's
quick actions + Notifications link actually navigate — so the already-wired Trips, Notifications,
and Profile screens are reachable in the running app for the first time. Routes live at
`src/app/{trips,profile,notifications,documents,vehicle,settings}.tsx`.

**Driver trip flow — fully wired end to end, straight-through** via expo-router (`src/app/trip/*`):
Dispatched →(inline)→ AtPickup →`/trip/pickup` (cargo photo)→ InTransit →`/trip/navigate`
(live map, GPS auto-detects arrival within 200m, manual "I've Arrived" fallback)→
AtDelivery →`/trip/delivery` (POD photo)→ Completed →`/trip/completed` (summary). No more
detour back to Home between pickup and delivery. Home still routes into each step by status
and refetches on focus (for a driver who backgrounds the app mid-flow).

---

## 5. Verification & ops status
- **Dev environment stood up (2026-08-17)**: `https://dev.mercon.tech` now runs a
  fully isolated second stack on the same VPS as prod (`dev-api`/`dev-frontend`/`dev-postgres`,
  ports 3051/3061/15433, its own `mercon-dev_pgdata` volume and Postgres credentials, its own
  `JWT_SECRET`), deployed automatically by `.github/workflows/ci-cd-dev.yml` on every push to
  the `dev` branch via `docker compose -p mercon-dev up -d --build`. The explicit `-p mercon-dev`
  project name is what keeps it from colliding with prod's `mercon` compose project — both
  workflows check out into the same self-hosted-runner work directory. Along the way, fixed a
  real bug in `docker-compose.yml`: the uploads bind mounts (`/tmp/mercon-uploads`, `/tmp/uploads`)
  were hardcoded regardless of `CLIENT_NAME`, so a second stack would have shared prod's uploaded
  documents on disk — both are now parameterized (`TMP_UPLOADS_DIR` / `CLIENT_NAME`), with
  defaults verified to keep prod's existing paths byte-identical (`docker compose config` diffed
  before/after). nginx: `/etc/nginx/sites-available/dev.mercon.tech.conf` (hand-created, not
  repo-managed — same precedent as `mesiri.mercon.tech`/`mesiriweb.mercon.tech` on this box),
  TLS via `certbot --nginx -d dev.mercon.tech`. Verified: containers healthy, `/api/health` OK,
  seeded dev admin login returns a JWT, prod containers' uptime unaffected by the dev deploy.
- Backend `tsc`: clean · Web `tsc --noEmit`: clean · Mobile `tsc --noEmit`: 0 errors (as of last session).
- Trip creation (driver required, auto-dispatch, availability conflict, driver notification) verified end-to-end against a local Postgres + running API (manual curl pass, test rows cleaned up).
- Web location picker (Nominatim address search + Leaflet pin drop/drag, recenter-on-search) verified in-browser.
- `react-native-maps` added (mobile) for the new live-map screen; not yet run in a simulator/device — see below.
- ❌ **Not yet run on a real phone against the live server** — this is the next real check.
- Git identity reminder: ensure commits use `sayedhysampm@gmail.com`.

---

## 6. Full-stack audit remediation (2026-07-29)

A full audit (backend/web/mobile/deploy) found 6 critical, ~13 high, ~10 medium, and
several low-severity issues. Fixing in phases per `~/.claude/plans/hidden-painting-deer.md`
(local plan file, not in repo). Status:

- ✅ **Phase 0 — Credential rotation (code)**: `docker-compose.yml` now requires
  `POSTGRES_USER`/`POSTGRES_PASSWORD` as GitHub secrets (was hardcoded `mercon_user`/
  `mercon_password`), Postgres port bound to `127.0.0.1` only (was public on `15432`),
  `ci-cd.yml` passes the new secrets through with the same abort-if-unset guard as
  `JWT_SECRET`. **Owner action still needed**: generate a real password, add
  `POSTGRES_USER`/`POSTGRES_PASSWORD`/`SEED_ADMIN_PASSWORD` as GitHub Actions secrets
  before the next deploy, or the container will fail to start.
- ✅ **Phase 1 — Critical, item 1**: `prisma/seed.ts` rewritten to seed only the two
  canonical accounts (`admin`, `operator`) per this file's own rules — no more fake
  customers/drivers/vehicles/trips/invoices seeded into production on every deploy.
  **Not done yet**: already-seeded fake rows already in the production DB are untouched;
  owner needs to review and manually clean those up.
- ✅ **Phase 1, item 2**: `POST/PUT /users` now Zod-validates `role` to `Admin|Operator`
  only — previously any string was accepted, so an Admin could set `role: "Driver"`
  through User Management, contradicting the "Driver users must not be creatable there"
  rule.
- ✅ **Phase 1, item 3**: Socket.IO now requires a valid JWT in the connection handshake
  (was fully open — anyone could connect and listen to every live GPS/notification
  event). Sockets auto-join a private room by identity; trip GPS relay validated against
  the sending driver's own assigned trip and scoped to a `trip:<id>` room; dashboards/
  drivers must `join:trip` (authorized server-side) to receive it.
- ✅ **Phase 1, item 4**: Driver `EmergencyScreen` was fully built but unreachable (no
  route, no nav entry) and used a stale `navigation` prop that would have silently no-op'd
  even if reachable. Fixed: converted to `expo-router`'s `useRouter`, added a
  `/trip/emergency` route, added an SOS button on the driver Home screen header and on
  `LiveNavigationScreen`'s top bar, and `send()` now attaches the device's current GPS
  coordinates.
- ✅ **Follow-up (owner-approved)**: `TripTrackingPage.tsx` (web) was found to no longer
  use a real socket connection — it rendered simulated telemetry
  (`useSimulatedTelemetry`/`PREDEFINED_ROUTES`) instead of live GPS, with fabricated
  "Riyadh Dry Port"/"Jeddah Islamic Port" location names and a fake ETA/progress bar on
  every trip regardless of actual route. Rewired to the real authenticated Socket.IO
  connection (`join:trip`, listens on `trip:location_update:<id>`), shows real pickup/
  dropoff coordinates instead of hardcoded port names, and a real "LIVE"/"DISCONNECTED"
  status + "last update" timestamp instead of the always-green fake indicator. Verified
  end-to-end locally (simulated a driver GPS emit over an authenticated socket, confirmed
  the truck marker/speed/timestamp updated on the dashboard in real time).
- ✅ **Follow-up (owner-approved)**: Emergency incident photos now actually reach the
  backend — added `DocType.Emergency` to `schema.prisma` (additive enum value, pushed
  locally via `prisma db push`, will auto-apply on next production deploy per this repo's
  existing `prisma db push --accept-data-loss` deploy step), added multipart upload
  (`upload.array('photos', 4)`) to `POST /mobile/emergency`, and the endpoint now creates
  a `Document` per photo linked to the driver's active trip. Verified end-to-end via curl
  (uploaded a real file, confirmed the `Document` row was created with the right
  `entity_id`/`doc_type`).
- ✅ **Phase 2 (High) — items 11–17 (session continued)**:
  - **Item 11** (mobile global 401 handling): `api.ts` response interceptor now clears
    SecureStore + routes to `/login` on any 401 — done in previous session.
  - **Item 12** (duplicate photo upload on retry): `PickupVerificationScreen` and
    `DeliveryVerificationScreen` now track uploaded-photo indices in a `useRef<Set<number>>`;
    a retry only re-uploads photos that failed, not those already successfully sent. Also
    added a `inFlight` ref to prevent double-tap re-entrancy (React state updates are async,
    so a ref guard is needed in addition to the `submitting` state). Also fixed
    `camera.ts` which had been accidentally doubled (all functions declared twice) — removed
    the stale second copy; the first copy (with `expo-image-manipulator` resize to 1280px)
    is the correct one.
  - **Item 13** (web 401 vs 403 split): `src/lib/api.ts` now only force-clears the session
    on 401 (expired/revoked token). A 403 (authorised user, wrong role) is now rejected
    as a normal error so the calling page can show an inline "You don't have permission"
    message instead of silently logging the operator out.
  - **Item 14** (block expired-license driver onboarding): `AddDriverPage.tsx` now includes
    `isExpiryValid` in `isFormValid` and in `handleSubmit`, blocking onboarding when the
    license is already expired. Same check added to `CreateDriverModal.tsx` (the inline
    "Add Driver" modal on Create Trip page).
  - **Item 15** (cargo type field): *(superseded 2026-08-07 — the cargo type dropdown
    described below was never actually wired in; `CreateTripPage.tsx` still submits a
    hardcoded `'General Goods'`, and the hazmat badge/flag mentioned here was removed
    project-wide, see top of file)*
  - **Item 16** (time ordering check): `CreateTripPage.tsx` now validates
    `dropoffTime > pickupTime` before submission and shows a clear error with the form
    switching back to the Route tab.
  - **Item 17** (invoice subtotal from rate card): `CreateInvoicePage.tsx` now fetches the
    selected customer's active rate card on trip selection and auto-fills the subtotal from
    `base_price`. The field stays editable; an "Auto-filled from rate card — editable" badge
    appears and the input gets a green border when auto-filled.
- ✅ **Phase 3 (Medium) — completed (session continued)**:
  - **Item 20** (sendBulkCommunication): UI updated to clearly state "Not connected to SMS provider" to avoid user confusion.
  - **Item 21** (Reports pagination): Modified `reportsController`, `reportsService`, `FleetPerformancePage`, and `DriverPerformancePage` to handle pagination properly and extract data array.
  - **Item 22** (isError handling/banners): Systematically updated `DataTable.tsx` references across all major list pages (Drivers, Vehicles, Trips, Invoices, Rate Cards, Customers, User Management) and `DashboardPage` to handle errors correctly.
  - **Item 23** (Sidebar notification badge): Wired `Sidebar.tsx` to use react-query and `notificationService` to display real unread notification counts instead of a hardcoded value.
  - **Item 24** (Trip-status transition guard): Implemented transition guard in `TripListPage.tsx` to prevent selecting invalid statuses for a trip, mirroring the backend validation. (`TripDetailsPage.tsx` was verified to already be safe).
  - **Item 25** (Attach JWT to Socket.IO in TripTracking): Verified `TripTrackingPage.tsx` already attaches JWT to `auth.token` and has a live status indicator.
  - **Item 26** (CI/CD health-check for docker): Added `pg_isready` healthcheck to `postgres-db` and `curl` healthcheck to `mercon-api` in `docker-compose.yml`.
  - **Items 27 & 28** (TLS/HSTS and tighten CORS): Installed `helmet`, configured HSTS, and restricted CORS to specific dashboard origins in `backend/api-server/src/index.ts`.
- ✅ **Phase 4 (Low/Cleanup) — completed (session continued)**:
  - **Item 29** (Add Zod to createMaintenanceRecord): Added Zod schema validation to `maintenanceController.ts`.
  - **Item 30** (Consolidate multer configs): Removed duplicate `multer` config from `uploadController.ts` and reused the one in `middlewares/upload.ts`.
  - **Item 31** (Add index on Trip): Added `@@index([vehicleId, driverId])` to the `Trip` model in `schema.prisma`.
  - **Item 32** (Dead code removal): Confirmed unused routes (like trackingRoutes) are deleted.
  - **Item 33** (Replace implicit any): Replaced `any` with `Prisma.TripWhereInput` in `tripController.ts`.
  - **Item 34** (Improve mobile NavigationCard UI padding): Improved bottom padding on the `LiveNavigationScreen` to handle safe area constraints gracefully.
  - **Item 35** (Standardize error response shape): Refactored `uploadController.ts` to follow the standard `error: { code: '...', message: '...' }` pattern.

---

## 7. Error-handling & observability audit remediation (2026-09-19)

A full error-handling/logging/observability audit (backend + web dashboard + mobile app +
infra) scored the system ~2.8/10, Level 2 maturity — no request correlation, no production
error monitoring anywhere in the stack, two endpoints leaking raw stack traces to clients, a
hardcoded third-party API key fallback, and no process-level crash handling. Full findings
and a 3-phase roadmap were written up; **Phase 1 (backend-only, low-effort, security/reliability
critical) is done**, on branch `ilan`, targeting `dev` first — production promotion is a
separate, explicit step via `promote-dev-to-prod.yml`, not part of this change:

- ✅ Removed the hardcoded Gemini API key fallback literal in
  `services/ocrService.ts` — now reads `env.GEMINI_API_KEY` only, falls back to regex
  extraction (existing behavior) when unset. **Owner action still needed**: rotate that key
  in Google AI Studio — it was committed to source and must be treated as compromised.
- ✅ Added a request-correlation system: `middlewares/requestContext.ts`
  (`AsyncLocalStorage`-based, `crypto.randomUUID()` per request, `X-Request-Id` response
  header, `req.id`), wired into `utils/logger.ts` via a pino `mixin()` so every existing
  `logger.error/warn(...)` call anywhere in any controller automatically carries the request
  ID with zero per-call-site changes.
- ✅ `tripController.ts` `getTrips`/`getTripById`: no longer return `stack`/`details` (a raw
  Prisma/Node stack trace) to API clients on a 500 — client now gets a generic message +
  `requestId` to correlate with the server-side log line, which still has the full error.
  Removed a duplicate `console.error` next to the existing `logger.error` in `getTripById`.
- ✅ `tripController.ts` `createTrip` failure log no longer writes the full `req.body`
  (address/contact PII) to stdout — logs `customer_id`/`driver_id`/`co_driver_id`/`vehicle_id`
  only.
- ✅ `index.ts`: added `process.on('unhandledRejection'/'uncaughtException')` (log fatal;
  `uncaughtException` exits so Docker's `restart: unless-stopped` + healthcheck recycle the
  process cleanly instead of continuing in a possibly-corrupt state). Wrapped the two
  previously-unguarded async Socket.IO handlers (`join:trip`, `driver:location_update`) in
  try/catch so a transient DB error there fails the one socket event, not the whole process.
- ✅ Global fallback error middleware in `index.ts` now includes `requestId` in its JSON
  response too, for consistency with the two fixed `tripController.ts` responses.

`tsc --noEmit` clean. `npm run build`'s `prisma generate` step hit a pre-existing local
Windows file-lock (`EPERM` renaming `query_engine-windows.dll.node`) unrelated to this
change — not something to chase for a backend-only TS change; `tsc --noEmit` is the
meaningful check here and passed.

**Phase 2 (backend + web dashboard) — done, same session.** Scope: an `error_events` table
plus an Admin-only Error Console, built so the ~35 controllers' existing `logger.error({err},
...)` calls populate it automatically with zero call-site rewrites:

- ✅ `prisma/schema.prisma` — new additive `ErrorEvent` model (`error_events` table):
  fingerprint (deduped), code, message, stack, route, source (`api`/`web`), count, status
  (`New`/`Acknowledged`/`Resolved`), lastRequestId, firstUserId, notes. `npx prisma db push`
  applied locally; production picks it up on next deploy via the existing
  `prisma db push --accept-data-loss` step.
- ✅ `middlewares/requestContext.ts` extended (route + userId, set from `middlewares/auth.ts`
  once the caller is resolved) so error captures carry rich context with no call-site changes.
- ✅ `services/errorCapture.ts` — dedup-by-fingerprint upsert into `error_events`; never logs
  through `logger` itself (would recurse into the hook below), only `console.error` on its own
  failure.
- ✅ `utils/logger.ts` — added a pino `hooks.logMethod` that fires `captureError()` for every
  `logger.error`/`.fatal({ err, ... })` call project-wide (level ≥ 50), automatically. Verified
  live: identical errors from the same throw site collapse into one row with `count`
  incrementing, not duplicate rows.
- ✅ `utils/errors.ts` — `AppError`/`NotFoundError`/`ConflictError` + `mapPrismaError` (P2002→
  Conflict, P2025→NotFound). Provided for new/touched code to adopt going forward — **not**
  retrofitted across the other ~210 existing `res.status(500)` call sites this round.
- ✅ New `GET/PATCH /api/error-events`, `GET /api/error-events/:id` (Admin-only via
  `authorizeRoles('Admin')`, SuperAdmin passes through) and `POST /api/client-errors`
  (authenticated, any role) — all verified live end-to-end (list/get/patch, and a 403 for an
  Operator token).
- ✅ Web dashboard: `/settings/error-console` (list, filterable by status/source) and
  `/settings/error-console/:id` (detail: message, stack, occurrence count, first/last seen,
  request id, status + notes editor), gated `RequireRole roles={['Admin']}`, linked from the
  Sidebar's Account group. `ErrorBoundary.componentDidCatch` and a new global
  `window.onerror`/`unhandledrejection` listener (`lib/errorReporting.ts`, installed once from
  `main.tsx`) both report to it.
- ✅ **The actual UX fix from the audit**: `lib/api.ts`'s `extractApiErrorMessage` no longer
  forwards the backend's raw message for any `>= 500` response (that's what produced the
  `"Database / Schema Error: ..."` leak) — now always a generic message + `(Ref: <X-Request-Id>)`
  using the header Phase 1 already sets on every response. Client errors (4xx) are unchanged —
  those are intentionally user-facing.

Slack/webhook alerting intentionally deferred (needs a webhook URL from the owner). Mobile app
changes (RN error boundary, crash reporting, upload-retry UX) deferred to a follow-up round —
mobile still has neither an error boundary nor crash reporting, and still silently swallows
failed proof-of-delivery photo uploads via `console.warn` (unchanged from Phase 1's note).
Also still not done: retrofitting `AppError`/`mapPrismaError` across the other ~210 existing
call sites, and fingerprint-based rate limiting of the capture path under a true error storm.

**Note (unrelated to this change, found while working)**: at commit time,
`frontend/web-dashboard/src/pages/customers/AddCustomerPage.tsx` and `EditCustomerPage.tsx`
had unstaged local modifications (removing `trade_alias`/`industry`/`cr_number`/`vat_number`/
`email`/`billing_address` fields, capping contacts at 2) that this session did not make and
left untouched/uncommitted — flagged to the owner rather than bundled into this commit.

---

## 8. Round-trip stop data corruption + timeline consolidation (2026-09-19)

Investigated a user-reported round trip (TRP-0252) that showed a different, wrong route on
three surfaces: web Trip Details ended at "Medina" instead of the real return destination
"Riyadh"; the driver app showed a phantom extra "Intermediate Stop" that was never configured;
the trip creation wizard showed the correct 4-point route. Root-caused to two separate bugs,
both fixed on branch `ilan`:

- ✅ **Data corruption at creation (the actual root cause)** — `tripController.ts`
  `bulkImportTrips` (the endpoint the wizard's "Review & Confirm" step and CSV import both call
  via `POST /api/trips/bulk-import`) unconditionally re-parsed the legacy `origin`/`destination`
  display strings via `parseFullTripStops()` even when the request already carried a correct,
  structured `stops[]` array (leg_index/stop_type-aware, built by
  `useTripSubmission.ts`) — then spliced that re-parsed function's first and last stops onto the
  front and back of the real array. Every trip created through the wizard got a duplicate origin
  stop prepended and a duplicate destination stop appended (worse on round trips: the duplicate
  destination stop lands *after* the true return-leg stops, corrupting `stop_sequence` order).
  Fixed: `row.stops` is now used as-is whenever present; `parseFullTripStops` only runs as a
  fallback when the client sends no structured stops at all. Also wired `validateTripStops`
  (leg_index/sequence invariant checks — existed, but only in the separate, less-used
  `createTrip` endpoint) into `bulkImportTrips` so this class of corruption fails loudly instead
  of silently writing bad rows going forward. **Historical trips created before this fix (e.g.
  TRP-0252) still have the corrupted stop rows in the DB — not retroactively cleaned up; needs a
  data migration if the owner wants existing trips fixed.**
- ✅ **Render-side consolidation** — three independent, disagreeing algorithms existed for
  turning a trip's raw stops into a human timeline: a backend "mirror" of the mobile app's logic
  (test-only, never used by any endpoint), the actual mobile app's `routeParser.ts`, and the web
  dashboard's `VisualRouteProgress.tsx` (a third, city-name-matching approach). Promoted the
  backend mirror (`services/mobileTripLogic.helper.ts`, already covered by 24 test cases in
  `multiStopRoundTrip.test.ts`) to the single production source of truth, renamed
  `services/tripRouteTimeline.ts`, fixed its `isRoundTrip()` to match all 5 signals the real
  mobile logic checks (was missing 3). Both `getTripById` (web) and the mobile trip endpoints
  (`formatMobileTrip` in `mobileTripController.ts`) now attach a computed `route_timeline` to
  every trip API response. `VisualRouteProgress.tsx` renders it directly when present (falls
  back to its old derivation only when absent — e.g. the handful of other pages that show a trip
  preview from list data without the full detail payload). The mobile app's `routeParser.ts`
  prefers `trip.route_timeline` from the API the same way, keeping its own 5-strategy local
  parsing only as an offline/legacy fallback.
- ✅ Fixed a related bug in `HomeScreen.tsx` (driver app home card + scheduled-trips list): it
  picked "the" dropoff stop via `.find(stop_type === 'Dropoff')`, which returns the *first*
  Dropoff — on a round trip that's the outbound delivery, not the true final destination. Now
  takes the pickup from the front and the dropoff from the back of the sequence-sorted stops.

`tsc --noEmit` clean on backend, web-dashboard, and mobile-app. Backend test suite (127 tests
across `services/*.test.ts`, including all 24 round-trip architecture cases) passes.

---

## Update protocol

**This file is the project's status memory. Update it in the SAME change that alters reality:**

1. **When code/schema/endpoint/screen changes** — flip the matching ⬜/🔄/✅, move rows
   between §2 (Completed) and §3 (Next), and update the §1 TL;DR percentages if an area
   crossed a threshold.
2. **When a step is finished** — mark it ✅ and add the commit hash where useful.
3. **Always bump `Last updated`** at the top to today's date.
4. Keep it honest — verify against code, never mark ✅ from intent alone.

Detailed session narratives go in `docs/progress/<date>.md`. The forward-looking plan
and locked decisions live in `PLAN.md`. **This file = current state at a glance.**

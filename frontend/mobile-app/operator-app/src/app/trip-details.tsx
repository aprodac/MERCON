/** Route: /trip-details?id= — a single trip's detail view. */
import TripDetailsScreen from '@/features/trips/details/TripDetailsScreen';

export default function OperatorTripDetailsRoute() {
  return <TripDetailsScreen />;
}

/** Route: /customer-edit?id= — create (no id) or edit (with id) a customer. */
import CustomerEditScreen from '@/features/customers/screens/CustomerEditScreen';

export default function OperatorCustomerEditRoute() {
  return <CustomerEditScreen />;
}

/** Route: /driver-edit — add a driver; /driver-edit?id= — edit one. */
import DriverEditScreen from '@/features/drivers/screens/DriverEditScreen';

export default function OperatorDriverEditRoute() {
  return <DriverEditScreen />;
}

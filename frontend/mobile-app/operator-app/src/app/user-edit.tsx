/** Route: /user-edit?id= — add (no id) or edit a platform user. */
import UserEditScreen from '@/features/users/screens/UserEditScreen';

export default function UserEditRoute() {
  return <UserEditScreen />;
}

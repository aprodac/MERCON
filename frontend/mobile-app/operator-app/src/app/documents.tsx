/** Route: /documents — fleet documents list. */
import DocumentsScreen from '@/features/documents/screens/DocumentsScreen';

export default function DocumentsRoute() {
  return <DocumentsScreen />;
}

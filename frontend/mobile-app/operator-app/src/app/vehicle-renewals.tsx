/** Route: /vehicle-renewals — vehicle document expiry tracker. */
import VehicleRenewalScreen from '@/features/vehicles/screens/VehicleRenewalScreen';

export default function OperatorVehicleRenewalsRoute() {
  return <VehicleRenewalScreen />;
}

/** Route: /trip/stop — intermediate stop verification + photo uploads */
import StopVerificationScreen from '@/screens/StopVerificationScreen';

export default function StopRoute() {
  return <StopVerificationScreen />;
}

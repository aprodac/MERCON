/** Route: /trip/pickup — cargo photo + confirm pickup (AtPickup -> InTransit). */
import PickupVerificationScreen from '@/screens/PickupVerificationScreen';

export default function PickupRoute() {
  return <PickupVerificationScreen />;
}

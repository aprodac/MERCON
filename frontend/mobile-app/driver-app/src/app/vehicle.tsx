/** Route: /vehicle — the driver's currently assigned vehicle. */
import AssignedVehicleScreen from '@/screens/AssignedVehicleScreen';

export default function VehicleRoute() {
  return <AssignedVehicleScreen />;
}

/** Route: /notifications — driver notifications feed. */
import NotificationsScreen from '@/screens/NotificationsScreen';

export default function NotificationsRoute() {
  return <NotificationsScreen />;
}
